Drop real app screenshots here to replace the marketing mockups.

Save PNGs with these EXACT names (the landing page already points at them):

  board.png     — a project board (Kanban columns + cards)
  editor.png    — a note open in the block editor (headings, a task list, colours)
  canvas.png    — a canvas with some ink / a text box
  calendar.png  — the Calendar (month view)
  palette.png   — the Command palette open (press Ctrl+K)

Notes:
- Any file that's missing just falls back to the styled mockup automatically —
  so you can add them one at a time.
- Crop to the app content (no need for the browser window chrome). PNG preferred.
- After adding files, rebuild (npm run build) or just refresh the dev server.
