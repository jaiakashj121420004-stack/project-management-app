# memory.md — Current State

> **The living memory of this project.** Read this first every session to know where things stand. **Update it after every meaningful change, then commit to git** (see [CLAUDE.md](./CLAUDE.md)). For the full spec see [plan.md](./plan.md); for build steps see [prompt.md](./prompt.md).
>
> Keep this file lean: it tracks *state*, not specification. Don't paste design or architecture here — link to `plan.md`.

---

## 📍 Status

- **✨ NVEXIS EDITOR/CANVAS/LIBRARY POLISH + DEFERRED FEATURES SHIPPED (2026-07-14) — ⚠️ needs 3 migrations + build/commit on Windows.** A large polish push across notes, canvas and Library, all type-verified via review subagents (the in-session Linux mount serves truncated reads → no in-session build; verify with the user's Windows `npm run build`). **Notes editor:** restored **toggle/collapsible blocks** (Details re-added to the shared schema + a toolbar button + `/toggle`); **audio/video embeds** as a **Pro** feature (`NoteEmbed`/`NoteEmbedView`/`EmbedModal` reuse the canvas `embeds.ts` allow-list — YouTube/Vimeo/Loom/SoundCloud — UI-gated via `useIsPro`/`UpgradeModal`); **images in notes** (free) with **preset + drag-to-resize** + delete (`NoteImage`/`NoteImageView`, private `note-media` bucket); **insert-canvas link** with a back-to-note breadcrumb; **Markdown export** (`docToMarkdown` in `serialize.ts` + a Download button in the note header); **starter templates** via the slash menu (`noteTemplates.ts` — meeting/journal/brief/weekly); **emoji icons** on notes + folders (reusable `components/forms/EmojiPicker.tsx`, shown in the note header + folder tree + Library cards); **cover images** on standalone notes (Notion-style banner, reuses `uploadNoteImage`/`useNoteMediaUrl`). **Canvas:** **jump-to-element** from the Layers panel (click a row → `centerCamera`); **named Frame element** (new `'frame'` type in `elements.ts` + `frameSchema` + `createFrame`/`bottomZ`; `FrameVisual` draws a translucent labeled rect behind content; an **Add frame** toolbar button; an inline frame-name field; `elementMeta` + `ElementPatch` extended); **highlight** now uses the same clean swatch popover as notes. **Data model:** `notes.icon` / `notes.cover` + `folders.icon` columns; `LibraryItem` gained `icon`. **Touched:** `features/editor/{NoteEmbed,NoteEmbedView,EmbedModal,noteTemplates,serialize,EditorToolbar,extensions,nodes/*,suggestion/slashItems}`, `features/notes/{NoteEditor,NoteBlockEditor,noteMedia,api,useNotes}`, `features/library/{types,useLibrary,api,FolderTree,LibraryContents,LibraryItemCard}`, `features/canvas/{elements,elementRenderers,LayersPanel,CanvasEditor,CanvasToolbar,TextFormatToolbar}`, `components/forms/{EmojiPicker,ToolbarPopover}`, `components/feedback/ErrorBoundary`, `types/database`. **⚠️ Apply in the Supabase SQL Editor (all idempotent):** [`20260714180000_note_media.sql`](./supabase/migrations/20260714180000_note_media.sql) (note-media bucket + RLS, if not already), [`20260714200000_notes_folders_icon.sql`](./supabase/migrations/20260714200000_notes_folders_icon.sql), [`20260714210000_note_cover.sql`](./supabase/migrations/20260714210000_note_cover.sql). **No new npm deps.** **Deferred (still): go-live** (Dodo LIVE keys, KYC, legal) — the last milestone. **Next:** runtime-test on Windows, apply the 3 migrations, commit per slice.
- **🌙 OVERNIGHT AUTONOMOUS RUN (2026-07-14) — Phases 3(complete)/4/5/6 BUILT; marketing re-skin deferred. ⚠️ NOT yet built/committed on Windows — see [WAKEUP.md](./WAKEUP.md) for the ordered runbook (2 migrations + build + per-slice commits).** All written + type-verified (review subagents; fixed a `SegmentedToggle` icon bug), **not runtime-tested** (no build/DB access in-session). **Phase 3 completed:** canvas text boxes now use the shared block schema (`richText.ts` re-exports `@/features/editor`), gained `/`+`:` commands, block CSS in `canvasText.css`. **Phase 4 — sharing (chose reliable share + RLS over blind Yjs-for-notes to protect the working editor):** migration [`20260714120000_sharing.sql`](./supabase/migrations/20260714120000_sharing.sql) (`share_canvas`/`share_note`/`*_collaborators`/`user_id_for_email` RPCs, `note_members` + helpers, notes RLS incl. membership, realtime); new `features/sharing` (`ShareButton`/`SharePanel`/`useSharing`), wired on standalone notes + personal canvases; Library shows shared items at root (effective-folder fix); `database.ts` gained `note_members` + RPCs + `NoteMember`. **Phase 5 — canvas nav:** `features/canvas/{bounds.ts,CanvasMinimap.tsx}` — corner minimap + viewport rect + click-jump + Fit/Reset, wired into `CanvasEditor` (named Frames deferred — new CRDT element type too risky blind). **Phase 6 — extras (subset):** ⌘K command palette (`features/command-palette`, in `AppShell`) + Library title search (`LibraryContents` `search` prop); backlinks/export/templates/version-history deferred. **Marketing — BUILT:** `features/marketing/LandingPage.tsx` rewritten as **"Lodestar"** (celestial starlit hero + Nvexis prism guiding-star → parchment feature sections), new `lodestar.css` + `lodestar/Mockups.tsx` (faux app screens as stand-in "screenshots"); showcases every feature (boards, Library/block editor, canvas+live cursors, calendar, to-dos, ⌘K, search, reminders, PWA/offline, minimap, colours, comments/@mentions, sharing, RLS) + pricing + testimonials. Old `sections/*` now unused. `/pricing`/`/terms`/`/privacy` untouched. **Deferred:** go-live (human-gated: Dodo LIVE keys, KYC, legal). **No new deps this run.**
- **✍️ NVEXIS PHASE 3 — Notion-style block editor + custom colours — IN PROGRESS (slice 1 built 2026-07-13; ⚠️ needs `npm install` + migration + build/commit on Windows).** New shared **`features/editor`** module = the ONE Tiptap v3 block schema for the whole app (`extensions.ts`: StarterKit + Highlight[multicolor] + TextStyle/Color + hardened SafeLink + TaskList/TaskItem + Details toggle blocks + a `ListStyle` global-attribute for hyphen/lettered/roman list variants; `blockExtensions` + a `collabBlockExtensions` Yjs variant mirroring the canvas pattern). `BlockEditor.tsx` (lazy-loaded so Tiptap stays code-split) + `EditorToolbar.tsx` (bold/italic/underline/strike, **text-colour picker**, **highlight-colour picker**, link, H1–H3, bullet/ordered/**task** lists, **list-style** popover, **toggle** block, quote, code block, divider) + scoped `editor.css` (restores lists/headings/task-checkboxes/toggle/placeholder that Tailwind resets, incl. `[data-list-style]` → `list-style-type`). `serialize.ts`: `emptyDoc`/`renderBlockHtml`/`docToPlainText`/`isEmptyDoc` + a **`markdownToDoc`** legacy converter. **Notes migrated to blocks:** migration [`20260713140000_notes_block_content.sql`](./supabase/migrations/20260713140000_notes_block_content.sql) adds `notes.content_json jsonb` (Tiptap doc = source of truth; the old `content` text column becomes the plain-text search mirror; additive + backward-safe — a legacy note seeds the editor by parsing its markdown once, writes JSON on first save). `NoteEditor` rewritten (dropped the markdown textarea/preview) to render the lazy `NoteBlockEditor` → `BlockEditor`, autosaving `content_json` + `content` on a 700 ms debounce; `patchNote`/`useUpdateNote`/`useUpdateLibraryNote` + `database.ts` thread `content_json`. **New free deps (require `npm install`):** `@tiptap/extension-{task-list,task-item,placeholder,details,drag-handle-react,emoji}` + `@tiptap/suggestion` (all confirmed on the public registry; drag-handle is free in v3). Tiptap-API usage verified against the installed type defs (subagent) — zero API errors. **Slice 2 (NEXT, not yet built):** `/` slash-command menu (`@tiptap/suggestion`), bubble toolbar, emoji picker, drag-handle blocks, and pointing the **canvas** text boxes (`RichTextBox`/`TextLayer` + Yjs collab variant) at the shared config so canvas text gains the same blocks. **Deferred cleanup:** `notes/markdown.tsx` + `noteContentSchema` now unused.
- **📚 NVEXIS PHASE 2 — Library + unified folder tree + standalone notes — ✅ SHIPPED + VERIFIED LIVE (2026-07-13; migration applied in Supabase, committed `dc2864a`, auto-deployed on Cloudflare, confirmed working).** Notes are now project-OPTIONAL (like canvases already were) and a real file-explorer **Library** organises standalone notes + personal canvases in one nested folder tree. **Migration [`20260713120000_library_folders.sql`](./supabase/migrations/20260713120000_library_folders.sql):** new `folders` table (owner-scoped, self-nesting `parent_id`, `position`; a SECURITY DEFINER `folders_before_write` trigger stamps updated_at + keeps owner_id immutable + rejects a parent that isn't yours / self / a cycle; owner-only RLS). `notes` gains `owner_id` (backfilled from each note's project owner), a **nullable** `project_id`, and `folder_id` (`on delete set null`); its RLS is rewritten to **owner-OR-project-member** read and **owner (standalone) / project-editor (project note)** write — existing project notes keep working, standalone notes are FREE (no Pro gate). `canvas_notes` gains `folder_id` (personal canvases file into the Library; project_id/owner_id stay immutable, Pro gate unchanged). **Frontend:** a new **`features/library`** module — `LibraryPage` at **`/library`** (replaces the old global `/notes` + `/canvas` nav entries; both now redirect to `/library`, per-project Notes/Canvas TABS unchanged). Desktop = folder tree (`FolderTree`, infinite nesting, auto-expands the active path) beside a contents grid; **mobile = tap-to-drill-in** contents with breadcrumbs (tree hidden). `LibraryContents` = breadcrumbs + create toolbar (New folder / New note / New canvas — canvas Pro-gated via `UpgradeModal`) + a grid of subfolders + note/canvas cards. `LibraryItemCard` opens on click and has a kebab / right-click / long-press **menu** (Rename / **Move to…** / Delete); moves use a `MoveToDialog` folder picker (Phase-2 decision: menu-move now, drag-and-drop is a later polish pass) that disables a folder's own subtree; deletes confirm. Opening a note → the shared `NoteEditor` (now **decoupled** from `projectId` — takes `runUpdate`/`runDelete` mutations so project + library notes reuse it); opening a canvas → the lazy `CanvasEditor` (Konva stays code-split; the library barrel is Konva-free). Data layer: `library/{api,useLibrary,tree,schemas,types}` (folder CRUD + optimistic hooks; `['folders',userId]` + `['library-notes',userId]` caches; personal canvases reuse the canvas feature's `['canvas-all',userId]` cache via new `useMoveCanvasToFolder`/`useRenameCanvas`); `notes/api` gained `fetchStandaloneNotes`/`insertStandaloneNote` + `folder_id` on `patchNote`; `canvas/api` summary now selects `folder_id` + `insertIndependentCanvas`/`patchCanvas` accept it. `types/database.ts` gained the `folders` table + `owner_id`/`folder_id`/nullable `project_id` on notes and `folder_id` on canvas_notes (+ exported `Folder`). **Mobile-first** (definition-of-done): no side tree on phones, breadcrumb drill-down, long-press menu, ≥40px targets. **Shipped + confirmed working live** (migration ran clean → "Success, no rows returned"; Library + Canvas load; create/open/autosave verified). **Post-ship bugfix (2026-07-13):** collapsing the canvas **pen colour panel** blanked the whole app — the Phase-1 `PenToolbar` early-returned the collapsed compact bar **before** a `useEffect`, so toggling changed the hook count and React threw "rendered fewer hooks than expected." Fixed by moving the picker-outside-click `useEffect` above the `if (collapsed) return` (all hooks now run unconditionally). **Phase-2 backlog (deferred, non-blocking):** desktop drag-and-drop moves (menu-move ships now); delete the now-orphaned `features/notes/NotesHome.tsx` + `features/canvas/CanvasHome.tsx` (their routes moved into the Library — still exported from the barrels, dead code, not an error). **4 locked decisions honoured:** hybrid glass-over-parchment · Notion block editor is Phase 3 (standalone notes reuse the markdown editor for now) · ONE unified Library tree · phased delivery. **Roadmap:** P1 ✅ · **P2 ✅ (this)** · **P3 Notion-style block editor + custom colours (NEXT)** · P4 collab (notes + canvas sharing UI) · P5 canvas minimap/frames/outline · P6 extras.
- **🎨 NVEXIS REBRAND + notes/canvas expansion — IN PROGRESS (started 2026-07-13). Phase 1 done; Phase 2 next. Full plan: [NVEXIS-UPGRADE-PLAN.md](./NVEXIS-UPGRADE-PLAN.md).** The app was rebranded from the colorful "Aurora" look to the **Nvexis "Almanac"** brand — hybrid *glass-over-parchment*: oxblood (`#7A2A26` Day / `#C24A40` Night) on parchment `#ECE4D6` / ink `#181210`, Fraunces + Spectral + IBM Plex Mono type (Google Fonts, no Inter), warmed glass + paper grain, oxblood-only accent, retinted earthy per-project accents (no migration), official **Nvexis prism** icons (`public/brand/*` + `favicon`/PWA + sidebar Brand mark), editorial radii/contrast polish (every `text-white`→`--accent-fg`), and a **mobile pass** from real-device screenshots (board columns `85vw`, scrollable project tabs, **collapsible canvas pen panel**, top-bar search shrink, bottom-nav clearance). App name stays *Aurora* (a Nvexis product line); only the look changed. **4 locked decisions:** hybrid glass-over-parchment · Notion-style block editor (notes + canvas text) · one unified "Library" folder tree (notes + canvases) · phased delivery (marketing re-skin LAST). **Roadmap:** P1 rebrand+mobile ✅ · **P2 Library + folder tree + standalone notes (NEXT)** · P3 block editor + custom colours · P4 collab for notes + canvas sharing UI · P5 canvas minimap/frames/outline · P6 extras (Ctrl-K, search, backlinks, export, templates). Design law now in `DESIGN-GUIDELINES.md` (do not reintroduce violet/cyan/Inter). **Env note:** file tools (Read/Write/Edit/Grep) are reliable (host); **bash reads are stale/truncated** — verify via the user's Windows `npm run build`; commit/push happens on Windows; Cloudflare auto-deploys on push.
- **✅ Canvas creative upgrades — colours, precision eraser, document text (2026-06-30) — typecheck + lint + `vite build` all clean.** Three user-requested canvas improvements: **(1) Custom colour picker** — a new on-brand `ColorPicker` (preset swatches + a saturation/value spectrum + hue slider + hex field + localStorage "recent" colours; `color.ts` has the HSV⇆RGB⇆hex maths) replaces the fixed swatch rows. The pen toolbar gains a **custom-colour** popover (`Pipette` trigger) and the text format toolbar's colour popover now hosts the full picker. **(2) Precision eraser** — a second eraser mode alongside the whole-stroke one, chosen in a new contextual `EraserToolbar` (Object vs Precision + a nib-size slider). Precision erasing removes only the ink the nib passes over by removing sample points within a world-space radius and splitting the stroke into surviving pieces (`erasePartialStroke` in `freehand.ts`), committed as one undo step (keeps each piece's original z). **(3) Document-style text** — text boxes are now **borderless** (the Konva box is a transparent hit-target; only an EMPTY box shows a faint dashed hint), **auto-grow** in height as you type (a `ResizeObserver` in `RichTextBox` reports content height, persisted via a new non-undoable `syncElementLayout` on TEXT_SYNC_ORIGIN so it never pollutes undo), and on **ruled** pages every block (p/li/h1/h2/quote) is pinned to the 28px rule rhythm so wrapped lines land on the lines. A new **"Add a page to write on"** toolbar action (`FileText`) drops a document-width (720px) borderless writing column for Google-Docs-style long-form typing; text creation snaps its top to the nearest rule. New files: `features/canvas/{color.ts, ColorPicker.tsx, EraserToolbar.tsx}`; touched `PenToolbar, TextFormatToolbar, freehand, constants, CanvasStage, CanvasEditor, CanvasToolbar, elements, elementRenderers, RichTextBox, TextLayer, canvasText.css, collab/{yCanvasDoc, useYjsCanvas}`. No DB migration. Verified in a clean clone (typecheck + eslint + `vite build` clean; canvas chunk stays lazy + separate, 941 kB/290 kB gz). **⚠️ Build + commit pending on the user's machine** (in-session mount intermittently truncates file reads).
- **✅ Canvas live multiplayer — Pro P3.7 (2026-06-29) — built; typecheck + lint + `vite build` all clean.** The canvas is now **real-time collaborative** via **Yjs over Supabase Realtime**. The local scene became a **CRDT**: one `Y.Doc` per canvas note holds a `Y.Array<Y.Map>` `elements` (each element a Y.Map of its flat fields) + one top-level `Y.XmlFragment` per TextBox + a `meta` map (`pageType`). The old `useSceneHistory` snapshot stack was **replaced by `collab/useYjsCanvas.ts`** — a drop-in hook exposing the SAME `{ scene, commit, undo, redo, canUndo, canRedo }` surface (so the ~900-line `CanvasEditor` barely changed): `scene` is derived from the doc on every change (local **or** remote → live re-render); `commit(nextScene)` **diffs** the scene against the doc and applies minimal ops in one transaction; **`Y.UndoManager`** (scoped to local-origin element edits) replaces the snapshot undo so a peer's concurrent work is never clobbered. **Transport = a THIN custom provider** (`collab/SupabaseYjsProvider.ts`) over a **private** Realtime broadcast channel `canvas:<noteId>`: a state-vector sync handshake on join, base64 Yjs `update`s broadcast/applied live, and **awareness** (`y-protocols/awareness`) carrying cursor (world coords) + selection + name/colour/avatar → **live remote cursors + selection halos** drawn in a new HTML overlay `RemotePresenceLayer.tsx` (reuses the `Avatar` styling; reduced-motion aware). **Text** binds Tiptap to each box's fragment via `@tiptap/extension-collaboration` (+ `-collaboration-caret`), so two people type in one box and merge; the element's `body`/`text` stay a derived cache for the static renderer. **Persistence:** debounced autosave now also encodes `Y.encodeStateAsUpdate` → `canvas_notes.doc_state` (BYTEA, hex) **alongside** the denormalised `scene` jsonb; reopening re-seeds from `doc_state` (Yjs merges → last-write safe). **Gating:** only Pro canvases join, and the channel is **RLS-secured** (new migration `20260629120000_canvas_realtime.sql` adds `realtime.messages` policies — receive = `can_access_canvas`, send = `can_edit_canvas`). Media bytes never enter the doc (only storage path/embedUrl + transform). New files: `features/canvas/collab/{yCanvasDoc.ts, SupabaseYjsProvider.ts, useYjsCanvas.ts, awareness.ts, encoding.ts}`, `features/canvas/RemotePresenceLayer.tsx`; touched `CanvasEditor/CanvasStage/TextLayer/RichTextBox/richText/api/useCanvas`; `history.ts` is now **superseded** by `useYjsCanvas` and unused (`git rm` it — it's still on disk because the in-session mount can't delete). **⚠️ One-time: apply `20260629120000_canvas_realtime.sql` in the Supabase SQL Editor** (no dashboard/Storage step). **⚠️ Build + commit are pending on the user's machine** — this session's Linux mount serves **truncated working-tree reads** (the documented desync), so verification was run in a clean reconstructed tree (full source + these exact edits): `tsc -b`, `eslint`, and `vite build` all pass, and the canvas chunk (`CanvasEditor` 930 kB/287 kB gz, now carrying Konva+Tiptap-collab+Yjs+y-protocols) stays **lazy + separate** from the main bundle. The working tree itself was never committed from-session to avoid recording truncated files.
- **✅ Canvas element management — Pro P3.6 (2026-06-29) — build fixed, lint clean, verified live-ready.** Multi-select (Shift+click + marquee), group move, z-order (bring/send forward/backward + to-front/to-back), pin/lock, visibility toggle, duplicate, copy/paste, right-click + long-press (600 ms) context menu (`ContextMenu.tsx`), a layers/element-list panel (`LayersPanel.tsx`, select/reorder/lock/visibility), and full keyboard shortcuts (Ctrl/⌘+C/V/D, `[`/`]` ± Shift for z-order, arrows nudge ±Shift, Delete/Backspace; Ctrl/⌘+Z, Shift+Z/Ctrl+Y). **The earlier Cloudflare failure was already resolved by follow-up commits** `90b215d` (buildStroke missing `visible:true` — a real tsc error), `fa3ebab` (remove stray `lint.txt`), `09bd1c7` (complete a truncated `freehand.ts`); `tsc -b` and `vite build` pass clean on HEAD. **This session then cleared the last 5 ESLint errors** in the committed code: 3 ternary-as-statement shortcuts in the `CanvasEditor.tsx` keyboard handler rewritten to if/else, and 2 unnecessary `as Konva.Node | undefined` assertions dropped in `CanvasStage.tsx`. Now **typecheck + `eslint src` + `vite build` all clean**. Verified by exporting HEAD from the git object store into a clean dir and building there (the in-session Linux mount serves truncated file reads — the documented desync — so builds and the commit are driven from git objects, never the working tree). Touch/stylus parity intact (pen `pointerType` + palm rejection, long-press menu, two-finger pan/zoom). Committed + pushed as the new `main` head.
- **✅ Canvas audio + video LIVE — Pro P3.5 (2026-06-29).** Canvases now support **both** in-app record/upload **and** external embeds (the locked decision). **(1) Record/upload (file source):** an **Add media** toolbar button (`Clapperboard`) opens a **lazy-loaded** `AddMediaModal` (recorder code stays out of the canvas chunk until opened) with Record / Upload / Embed tabs. The **recorder** (`useMediaRecorder.ts`) wraps `MediaRecorder` (audio `audio/webm;codecs=opus`, video `video/webm;codecs=vp9`, with graceful fallbacks), handles the **mic/camera permission flow** (clear denied/no-device/unsupported messages + a "Try again"), ticks a **duration**, tracks recorded **bytes with a size meter against the per-type cap**, and **auto-stops** if the clip would exceed the cap. Upload accepts an audio/video file, validated against `MEDIA_CAPS` (audio 25 MB / video 100 MB). Both paths `uploadCanvasMedia` → `canvas-media` bucket (`<projectId>/<noteId>/<uuid>.<ext>`) and commit a **`MediaElement`** (`source:'file'`, storage `path` — never a data URL). **(2) Embed (embed source):** paste a URL → `embeds.ts` parses + validates against a strict **provider allow-list** (YouTube, Vimeo, Loom, SoundCloud) and rebuilds the **canonical** embed URL itself from the extracted id (never the raw string; non-http(s) and unknown hosts rejected) → stored as `MediaElement` (`source:'embed'`, `embedUrl`). **(3) Rendering:** Konva can't host players/iframes, so media draws via a new **HTML overlay `MediaLayer`** (mirrors `TextLayer`) — `<audio>`/`<video>` from a **signed URL** (now a shared `useSignedUrl.ts` cache, extracted from the image renderer) for files, a responsive **`<iframe>`** (`referrerPolicy=strict-origin-when-cross-origin`, `loading=lazy`, only the canonical allow-listed src) for embeds; Konva renders only the background rect (hit/transform/drag target). Players are **interactive only when selected (edit mode) or always in View/viewer mode**; otherwise click-through so Konva keeps select/drag. Resize handles stay grabbable via Transformer **padding** (video keeps aspect ratio); move a selected clip by deselecting then dragging. Drag-drop now routes audio/video too. Free users blocked by `<ProGate>`; Storage RLS is the real gate; personal canvases excluded (bucket is project-keyed). `proFeatures.media` description updated. **No DB migration** (the `MediaElement` shape, the 100 MB `canvas-media` bucket, and the audio/video caps already existed). New files: `features/canvas/{embeds.ts, MediaLayer.tsx, useMediaRecorder.ts, AddMediaModal.tsx, useSignedUrl.ts}`; touched `elements.ts`, `elementRenderers.tsx`, `CanvasStage.tsx`, `CanvasToolbar.tsx`, `CanvasEditor.tsx`, `lib/proFeatures.ts`. **✅ Built + committed + pushed + verified working live (commit `5914c2c`, 2026-06-29)** — auto-deployed via Cloudflare on push to `main`; record, upload, and all four embed providers confirmed on a live Pro canvas. (The build/commit ran on the user's Windows machine because this session's Linux sandbox mount served truncated file reads — the documented mount-desync — so in-session `tsc`/`eslint`/`vite build` were unreliable.) Follow-up lint sweep (2026-06-29): cleared all 8 remaining ESLint errors — removed `jsx-a11y/media-has-caption` disable directives for a rule this project doesn't configure (AddMediaModal, MediaLayer), dropped an unnecessary type assertion + a ref-update-during-render (`useMediaRecorder` cap), moved `setHtmlImg(null)` out of the image-load effect body (elementRenderers), and converted `TodoListCard`'s rename-sync effect to the adjust-state-during-render pattern. `npm run lint` should be clean (pending re-run; canvas files were being edited concurrently).

- **✅ Canvas images now live (Pro P3.4, 2026-06-29).** Images can be added to any project canvas via **file picker**, **paste** (Ctrl/⌘+V), or **drag-drop**. Files are validated against `MEDIA_CAPS.image` (PNG/JPEG/GIF/WebP, max 10 MB) before upload; oversized/wrong-type files show a dismissible inline error banner. The upload goes to the `canvas-media` Storage bucket (path `<projectId>/<noteId>/<uuid>.<ext>`) via `lib/storage.ts`; the `scene` jsonb stores an **`ImageElement`** with a `path` reference — **never a data URL**. Rendering uses a **module-level signed URL cache** (`urlCache`/`inFlight` Maps, 1-hour validity) to avoid duplicate fetches; while the URL or `HTMLImageElement` is loading a **skeleton** (dashed rect + "Loading…") is shown, and if the signed URL or image fails a labelled fallback is shown instead of disappearing. Images behave like any other element: move/resize (**aspect-ratio locked by default**; dynamic `keepRatio` on the Transformer), rotate, lock, z-order, undo/redo. **Personal canvases** (no `projectId`) are intentionally excluded — Storage RLS parses `projectId` from the path's first segment and personal canvases have no project. Free users see a `<ProGate>` upgrade CTA; Storage RLS is the real gate. typecheck clean. `proFeatures.media.shipped = true`. No migration needed.

- **✅ Canvas rich text now matches the full P3.3 spec (2026-06-29).** Text boxes gained **inline text colour** and **XSS-safe links** (sanitised http/https/mailto only) on top of the formatting set below, so canvas rich-text now covers the whole prompt: bold/italic/underline/strike/highlight, headings, lists, quote, **colour, and links**. typecheck + lint + build clean; no migration. (Build/commit blockers noted in the old entry below are resolved.)
- **🆕 Canvas text editing is LIVE + pen confirmed (Pro P3.3, 2026-06-26).** Text boxes are now real **rich-text** editors (Tiptap v3): double-click a box (or **+ Add text**, which opens straight into edit mode) to type, with a floating format toolbar — bold/italic/underline/strike/highlight, H1–H2, bullet & numbered lists, quote. Konva can't render per-range rich text, so text is drawn as an **HTML overlay** (`TextLayer`) transformed by the same camera as the Konva box — Konva still owns select/move/resize/rotate/lock/delete + the Transformer, and a live-transform channel keeps the overlay glued to the node mid drag/resize. Edits debounce into one undo step + flush on exit; content persists as Tiptap JSON in `element.body` (+ a plain-text mirror in `element.text`) — **no DB migration** (the `scene` jsonb already declared `body`). The **pen/draw/eraser** tools were already shipped in P3.2 and are intact; if they're not visible in the app, it's running a **stale build** — restart `npm run dev` or accept the PWA "new version → Reload". New files: `features/canvas/{richText.ts, TextFormatToolbar.tsx, RichTextBox.tsx, TextLayer.tsx}`; touched `elements.ts` (ElementPatch + body/text), `constants.ts` (ElementBox), `elementRenderers.tsx` (text = Konva bg only + dbl-click + live transform), `CanvasStage.tsx` (TextLayer overlay + editing wiring), `CanvasEditor.tsx` (editingTextId state), `styles/index.css` (.canvas-rich typography). **typecheck + lint clean.** ⚠️ **Build + commit pending on the user's Windows machine** — this session's Linux sandbox can't run the platform `@rolldown/binding` for `vite build` and the working tree is CRLF with a stale `.git/index.lock`.

- **✅ Canvas creation FIXED (2026-06-25).** Root cause was **not** the INSERT policy — it was the **read-back**: the app creates a canvas with `.insert(...).select()` → PostgREST `INSERT ... RETURNING`, whose read-back is gated by the **SELECT** policy. That policy called `can_access_canvas(id)`, a STABLE SECURITY DEFINER fn that **re-queries `canvas_notes`**, which doesn't see the just-inserted (uncommitted) row → 42501 "new row violates row-level security policy." Proof: setting the INSERT policy to `with check (true)` STILL 42501'd; loosening the **SELECT** policy fixed it. **Fix = migration [`20260625140000_fix_canvas_select_readback.sql`](./supabase/migrations/20260625140000_fix_canvas_select_readback.sql)**: rewrite the canvas_notes SELECT policy to check the row's own columns **inline** (`owner_id`/`project_id`/`id`) instead of re-querying via `can_access_canvas` — same security, but the read-back of a new row succeeds. (Also along the way: dropped a rogue `canvas_stamp_owner` BEFORE-INSERT trigger that an earlier "Debug with Assistant" attempt had added, and confirmed the policy set is the correct 4 via `20260625120000_fix_canvas_insert_rls.sql`.) **⚠️ One-time: apply `20260625140000` in the SQL Editor** (or it's already applied via the live restore). Frontend error-surfacing change (`features/canvas/errors.ts` + banners) still pending a commit.
- **Phase:** **Phase 10 (Polish & launch) — built in code (2026-06-21); pending the user's Stripe setup + a build/commit/push + applying the billing migration.** Adds a stunning **public marketing site** (`features/marketing`: animated `HeroSection` with a live faux-board preview, features, showcase rows, testimonials, pricing teaser) at `/` + `/pricing` + `/terms` + `/privacy`; **Stripe billing** (Free vs **Pro** $8/mo) via three Edge Functions (`create-checkout-session`, `create-portal-session`, signature-verified `stripe-webhook`) and a `features/billing` module (`BillingPage`, `UpgradeModal`, `PlanBadge`, `useBilling`); **server-enforced plan limits** (Free = 3 owned projects via a `projects` BEFORE INSERT trigger; the `profiles` billing columns are trigger-protected so only the service-role webhook can set `plan`). Routing was split so **`/` is the public landing** and the app dashboard moved to **`/boards`**. Migration `20260621210000_billing.sql`. The prior **post-launch polish pass (2026-06-21)** — UI/menu fixes, offline detection, invitation accept/decline (`b02d44b`) — also shipped. **Phase 10.1 (2026-06-21)** then added: **iOS/responsive safe-area** insets (notch/Dynamic Island/home-indicator), **bigger free limits** (10 boards + 3 members/board, both server-enforced), an in-app **feedback** feature (admin-only inbox via `is_admin()`), a **"From the CEO"** message tab (admin posts, all read), and a subtle "Made by J. Jai Akash" footer. Migration `20260621230000_collab_limits_feedback_ceo.sql`. See the done log + top decision-log entry.
- **Phase 9 (PWA & reminders) — done** + applied/deployed/verified (2026-06-21). The app is now a **full installable PWA** with **due-date reminders**. **(1) Installable PWA** — `vite.config.ts` got the complete manifest (name/short_name/description, `id`/`scope`/`start_url`, `display: standalone`, categories, three icons incl. a **maskable** one, and app **shortcuts** to Projects/Calendar/To-Do) plus a Workbox service worker (`registerType: 'prompt'`) that precaches the app shell (`globPatterns` js/css/html/svg/png/woff2, `navigateFallback: index.html` so SPA routes + offline reloads render). **(2) Icons** — generated by a **zero-dependency Node script** (`scripts/generate-icons.mjs`, hand-rolled PNG encoder over a violet→cyan→magenta aurora gradient + "A" glyph) → `public/pwa-192/512`, `maskable-512`, `apple-touch-icon-180`, `favicon.svg`. **theme-color** is a single `<meta id="theme-color">` kept in sync with the active theme by `applyTheme()` (+ pre-paint inline script), so the browser chrome / PWA status bar matches light/dark. **(3) Offline UX** — a localStorage-**persisted TanStack Query cache** (`PersistQueryClientProvider` + `src/lib/queryClient.ts`) gives a **read-only cached view** offline (wiped on sign-out for shared-device privacy); a `useOnlineStatus` hook drives a glass **OfflineBanner** ("you're offline, changes won't be saved"); a **PWAReloadPrompt** (`useRegisterSW`) shows "ready offline" + a one-tap "new version → Reload" toast. **Queued offline writes deliberately not built** (documented tradeoff). **(4) Reminders — "Both"** (user opt-in choice): **(a) Email** via a Deno **Edge Function** `send-due-reminders` (secret-gated, queries the `due_reminder_candidates` RPC, sends per-assignee Resend digests, calls `mark_reminders_sent`) on a **pg_cron + pg_net** daily schedule — reliable, works when the app is closed; needs one-time setup (migration + `--no-verify-jwt` deploy + `RESEND_API_KEY`/`REMINDER_FROM_EMAIL`/`CRON_SECRET` secrets + cron SQL, all in supabase/README). **(b) Browser notifications** — zero-setup `features/reminders` module (`useDueReminders` polls the user's due-soon assigned cards, fires `Notification` once per card+due-date, localStorage dedupe). Both toggled in **Profile → Reminders** (`ReminderSettings`: email/browser switches + lead-time select), backed by new `profiles.reminder_emails_enabled` / `reminder_lead_days`. **(5) Assignee picker** — a compact `AssigneeField` (project members) added to the card modal so reminders have a target; `assignee_id` now threads through `CardDetailValues` → `updateCardDetail`/`useUpdateCard`/`useUpdateCalendarCard`. Migration `20260621090000_reminders.sql`. typecheck/lint/build clean (bundle 952 kB / 276 kB gzip; SW precache 25 entries). **Migration applied + Edge Function deployed + email & browser reminders verified end-to-end (2026-06-21).**
- **Phase 8 (Collaboration) — done.** The app is now multi-user. A new `features/members` module adds: **(1) Invitations** — a project owner invites by **email** (editor/viewer) via an `invitations` table; a brand-new user is auto-joined by an `auth.users` signup trigger, and an already-registered invitee is resolved on app load via the `redeem_my_invitations()` RPC (both call the SECURITY DEFINER `redeem_invitations_for(user, email)`, which atomically moves matching invitations into `project_members`). **(2) Members UI** — a `MembersBar` in the project header (live avatar stack + a Share/Members button) opens a `MembersPanel` modal: roster with avatars + roles, owner-only invite / change-role (editor↔viewer) / remove, and pending-invitation management. **(3) Role-aware RLS** — content **reads** stay membership-gated, but **writes** on `columns`/`cards`/`checklist_items`/`labels`/`card_labels`/`notes` now require **owner/editor** via new `can_edit_project()` / `can_edit_card()` helpers; viewers are read-only **at the database**, and the UI mirrors this (board drag/compose/edit + the card modal + notes editor all go read-only for viewers). The owner's own membership is protected by a trigger (can't be demoted, removed, or its role granted to others). **(4) Realtime** — `useProjectRealtime` subscribes to Postgres changes for the active project (columns/cards/labels/card_labels/checklist_items/notes/project_members) and **debounce-invalidates** the matching TanStack caches (board/card-extras/notes/members), so other members' changes appear live with no flicker, no duplicates, and no cross-project leak. **(5) Presence** — `usePresence` (Realtime Presence) shows who's viewing the board (green-ringed avatars + a "N viewing" pulse). typecheck/lint/build clean. **Migration applied + Realtime enabled (2026-06-21).**
- **Phase 7 (Notes/docs per project) — done.** A new `features/notes` module adds a **Notes tab inside each project** (a Board | Notes segmented control on `ProjectPage`, URL-driven via `?tab=notes` so it deep-links). A `notes` table (id, project_id, title, content, updated_at, created_at) with **membership-gated RLS** (verbatim `is_project_member(project_id)` pattern) plus a `before update` trigger (`touch_notes_updated_at`) that stamps `updated_at` server-side (frontend untrusted). UI: a two-pane **list/editor** (`NotesPanel`) — a list of notes newest-edited-first with create, and a markdown **editor** (`NoteEditor`) with a **live preview** (side-by-side on desktop, Write/Preview toggle on phones), inline title rename, inline-confirm delete, and **debounced autosave** (700 ms idle, flush-on-unmount so an in-flight edit is never dropped) with a subtle Saved / Saving / Unsaved / error indicator. Markdown is rendered by a **self-contained, XSS-safe renderer** (`markdown.tsx` → React elements, no `dangerouslySetInnerHTML`, link hrefs allow-listed) — **no new dependency**. The global `/notes` route is now a project picker (`NotesHome`) that links to each project's Notes tab. One `['notes', projectId]` optimistic cache mirrors the board/todos pattern. typecheck/lint/build clean. **Migration applied (2026-06-21).**
- **Phase 6 (Calendar view) — done.** `features/calendar` renders every card with a `due_date` across all/one project, Month/Week toggle, drag-to-reschedule (cross-cache optimistic), responsive grid↔agenda. (Full detail in the done-log.)
- **Earlier 2026-06-20 batch:** checklist/label add bugfix, deeper 3D buttons, open-ended task priority, daily to-do planner. Migrations `20260620100000_card_priority.sql` + `20260620120000_todos.sql` — both applied live.
- **Pro setup status (2026-06-22):** ✅ **All Pro migrations applied** in Supabase (P0 `pro_foundation`, `dodo_billing`, P1 `custom_reminders`, P2 `collaboration_pro`, + the earlier `billing` / `collab_limits_feedback_ceo`), **pg_cron at `*/10`**, and a **Dodo TEST checkout verified end-to-end** (webhook → Pro). The sub-bullets below are now **DONE** (kept as a record). **🚫 Go-live (Dodo KYC, refund/contact pages, finalize legal, TEST→LIVE switch) is DEFERRED to the very end, after P3 — see the top decision-log entry.** **⚠️ NEW one-time step (standalone canvases):** apply [`20260622200000_canvas_standalone.sql`](./supabase/migrations/20260622200000_canvas_standalone.sql) in the Supabase SQL Editor — adds `canvas_notes.owner_id` (+ backfill from the project owner) and makes `project_id` nullable, creates the `canvas_members` table, the five canvas access helpers, the immutability trigger, and rewrites the canvas RLS for project-or-personal ownership (no Storage/Realtime/dashboard step). It assumes the prior [`20260622180000_canvas.sql`](./supabase/migrations/20260622180000_canvas.sql) was applied first. **P3.2 (freehand) ✅, P3.3 (rich text) ✅, P3.4 (images) ✅, P3.5 (audio/video + embeds) ✅, P3.6 (element management) ✅, P3.7 (Yjs realtime co-editing) ✅ — all shipped (see done log).** **⚠️ NEW one-time step (P3.7 realtime):** apply [`20260629120000_canvas_realtime.sql`](./supabase/migrations/20260629120000_canvas_realtime.sql) in the Supabase SQL Editor — it adds two `realtime.messages` RLS policies that authorise the private `canvas:<noteId>` broadcast channel (receive = `can_access_canvas`, send = `can_edit_canvas`); no Storage/Realtime-publication/dashboard step (canvas tables don't join `supabase_realtime` — the doc syncs over broadcast, not Postgres CDC). Next build: **canvas sharing UI** (the `canvas_members` panel), then the deferred go-live. Historical one-time steps (already completed):
  - **Pro P2 collaboration (NEW):** (a) apply [`20260622160000_collaboration_pro.sql`](./supabase/migrations/20260622160000_collaboration_pro.sql) in the Supabase SQL Editor — it creates `comments`/`comment_mentions`/`reactions`/`activity_log`/`notifications`, the `cards.review_*` columns, all RLS + SECURITY DEFINER triggers, and **adds the 5 new tables to the `supabase_realtime` publication** (REPLICA IDENTITY FULL, idempotent — no dashboard step if Realtime is already on, like Phase 8). (b) For **email** notifications (optional): **redeploy** the existing function — `supabase functions deploy send-due-reminders --no-verify-jwt` — it gained a 3rd pass that emails un-emailed mention/reply/review notifications via the new service-role RPCs (no new secret; reuses Resend + `CRON_SECRET` + the 10-min cron). The Topbar bell (in-app) needs nothing beyond the migration. See [supabase/README.md](./supabase/README.md) → "Pro collaboration".
  - **Pro P1 timed reminders (NEW):** (a) apply `20260622140000_custom_reminders.sql` in the Supabase SQL Editor (`cards.due_at` + `card_reminders` + `card_reminder_dispatches` + the two new RPCs); (b) **reschedule pg_cron** for `send-due-reminders` from daily to **`*/10 * * * *`** and **redeploy the function** (`--no-verify-jwt`) so it runs the new precise path — both documented in [supabase/README.md](./supabase/README.md) → "Due-date email reminders". (Browser-channel timed reminders need nothing server-side.)
  - **Billing (Dodo Payments — Dodo TEST mode):** (1) apply ALL other pending migrations — `20260621210000_billing.sql`, `20260621230000_collab_limits_feedback_ceo.sql`, `20260622000000_pro_foundation.sql` (`project_is_pro()` + `canvas-media` bucket + Storage RLS), **and `20260622120000_dodo_billing.sql`** (`stripe_*`→`dodo_*` columns + extended guard trigger); (2) create the two Dodo **test-mode** subscription products (monthly + annual) — test ids `pdt_0NhalBvSKlS70L1sUMkur` / `pdt_0Nhalv2OMQi73YMxXuxm8`; (3) deploy the 3 Edge Functions (`dodo-webhook` with `--no-verify-jwt`); (4) add the Dodo webhook endpoint (`…/functions/v1/dodo-webhook`) + copy its signing secret; (5) `supabase secrets set` DODO_PAYMENTS_API_KEY / DODO_WEBHOOK_SECRET / DODO_PRODUCT_PRO_MONTHLY / DODO_PRODUCT_PRO_ANNUAL / APP_URL — all in supabase/README → "Dodo Payments billing". Then test a checkout in Dodo test mode. For production, also set `DODO_PAYMENTS_ENVIRONMENT=live` + live key/products/webhook (README go-live checklist). Non-blocking: production email (Resend domain + `REMINDER_FROM_EMAIL`) and bundle code-splitting.
- **Repo:** github.com/jaiakashj121420004-stack/project-management-app *(`main` pushed through **Pro P3.6** — element management built, Cloudflare build fixed, lint clean, 2026-06-29)*
- **Environment:** GitHub · Supabase · Cloudflare · Node.js — ready ✅ (see [SETUP.md](./SETUP.md))
- **App is live on Cloudflare Pages:** ✅ https://project-management-app-dev.pages.dev — React (Vite) preset, `npm run build` → `dist/`, both `VITE_` env vars set; auto-deploys on push to `main`. Verified 2026-06-21: app loads + connects to Supabase.
- **Last updated:** 2026-07-13 (**Nvexis Phase 2 — Library + unified folder tree + standalone notes.** New `folders` table + `notes` made project-optional (`owner_id`/nullable `project_id`/`folder_id`, RLS rewritten owner-or-member) + `canvas_notes.folder_id`, all in migration `20260713120000_library_folders.sql`. New `features/library` module: `/library` route (replaces the global `/notes`+`/canvas` nav, both redirect; per-project tabs unchanged) with a desktop folder tree + mobile drill-down contents, breadcrumbs, create folder/note/canvas, rename/move(menu)/delete, opening notes in the shared markdown `NoteEditor` (decoupled from projectId → takes runUpdate/runDelete) and canvases in the lazy `CanvasEditor` (Konva stays code-split). Standalone notes FREE; canvas creation Pro-gated. **Shipped: migration applied, committed `dc2864a`, deployed, verified working live.** Post-ship fix: canvas **pen-panel collapse** no longer blanks the app (a `PenToolbar` hooks-order bug — `useEffect` moved above the collapsed early return). Backlog (deferred): desktop DnD moves + deleting orphaned `NotesHome`/`CanvasHome`. Details in the top Status bullet. Earlier: **Pro P3.7 — canvas live multiplayer (Yjs over Supabase Realtime).** Converted the local scene to a CRDT: one `Y.Doc`/note (`Y.Array<Y.Map>` elements + a `Y.XmlFragment` per TextBox bound to Tiptap via `@tiptap/extension-collaboration` + `-collaboration-caret` + a `meta` map for pageType), `useSceneHistory` → `collab/useYjsCanvas.ts` (same surface; `commit`=scene-diff, undo=`Y.UndoManager`), a thin custom provider over a **private** Realtime broadcast channel `canvas:<noteId>` (state-vector sync handshake + base64 Yjs updates + `y-protocols/awareness` cursors/selection → live remote cursors + halos in `RemotePresenceLayer.tsx`), debounced `Y.encodeStateAsUpdate` → `canvas_notes.doc_state` BYTEA **alongside** the `scene` jsonb, and an RLS-secured channel (migration `20260629120000_canvas_realtime.sql`: receive=`can_access_canvas`, send=`can_edit_canvas`). Media bytes never enter the doc. typecheck + lint + `vite build` clean (verified in a clean reconstructed tree; canvas chunk stays lazy + separate, 930 kB/287 kB gz). **⚠️ Apply `20260629120000_canvas_realtime.sql`; build + commit pending on the user's machine — the in-session mount truncates working-tree reads.** Earlier: **Pro P3.6 canvas element management — Cloudflare build FIXED + ESLint cleaned + verified; typecheck, `eslint src`, and `vite build` all clean.** The build failure was resolved by follow-up commits `90b215d`/`fa3ebab`/`09bd1c7`; this session cleared 5 remaining ESLint errors in `CanvasEditor.tsx`/`CanvasStage.tsx`. Earlier: **Canvas audio + video — Pro P3.5.** Both record/upload (MediaRecorder → `canvas-media` → `MediaElement` file source, with permission UX + duration + size meter/auto-stop vs cap) and allow-listed embeds (`embeds.ts`: YouTube/Vimeo/Loom/SoundCloud → canonical embed URL, unknown hosts rejected → embed source). Players render in a new HTML overlay `MediaLayer` (`<audio>`/`<video>` via shared `useSignedUrl.ts`, responsive `<iframe>` for embeds); Konva draws only the bg rect. Lazy `AddMediaModal`. No migration. ✅ Built + committed + pushed + verified live (commit `5914c2c`). Earlier: **Canvas image elements — Pro P3.4.** File picker + paste + drag-drop; validate → upload to canvas-media bucket → ImageElement with storage path (never a data URL); signed URL cache; skeleton/error fallback; aspect-ratio-locked resize; personal canvas excluded. typecheck clean. Then **Canvas rich text completed to spec — Pro P3.3.** Added per-range **text colour** (Tiptap `TextStyle` + `Color`; an Aurora swatch popover in the format toolbar with a Default-colour reset) and **inline links** (a toolbar popover to paste/enter a URL, Apply/Remove, prefilled from the current link). Links are **XSS-safe by construction**: a shared `safeLinkHref` allow-list (http/https/mailto only) guards both the toolbar (invalid URLs disable Apply + show an inline error) and a hardened `SafeLink` mark that sanitises the href on **parse AND render**, so a stored `javascript:`/`data:` URL is dropped to *no href* before it can ever reach the static renderer's `dangerouslySetInnerHTML`; links render with `target="_blank" rel="noopener noreferrer nofollow"`. Verified with a DOM-shim render test (a `javascript:` and a `data:` link both render with no `href`; https/mailto preserved) plus **typecheck + lint + `vite build` all clean**. New dep `@tiptap/extension-link`; Color comes from the already-installed `@tiptap/extension-text-style`. Files: `features/canvas/{richText.ts, TextFormatToolbar.tsx}` (+ a small `.canvas-rich a` accent tweak left with the in-flight design WIP). No DB migration. Earlier 2026-06-26: (**Pressure-sensitive freehand drawing — Pro P3.2.** The canvas now has real drawing: `select` / **draw** / **erase** tool modes via **perfect-freehand**. Draw captures Stage pointer events into `[x,y,pressure]` world samples — real stylus pressure when the pointer is a pen, simulated otherwise (coalesced events for tablet smoothness) — paints the in-progress stroke **imperatively on a dedicated preview layer**, and commits it on pointerup as a **first-class `StrokeElement`** (selectable/movable/**resizable**/lockable/z-ordered/undo-able). A **pen toolbar** offers pen/marker/**highlighter** (translucent + multiply) presets, a colour palette, and a size slider; the **eraser** removes whole strokes (one undo per gesture); **palm rejection** ignores touch while a pen is active. P3.1 **pan/zoom/select untouched** (two-finger + wheel still work in every mode). New `features/canvas/{freehand.ts, drawing.ts, PenToolbar.tsx}`; Konva + perfect-freehand stay code-split (editor chunk 339 kB/105 kB gz; main bundle unchanged). No migration. typecheck + lint + build clean. **Earlier — Standalone canvases** — a canvas may now belong to a project (shared via project membership, as before) **OR** be a **personal** canvas with no project, owned by one user. Migration `20260622200000_canvas_standalone.sql`: `canvas_notes` gains `owner_id` (backfilled from the project owner, then `default auth.uid()` + `not null`) and `project_id` becomes **nullable**; a new **`canvas_members`** table (mirrors `project_members`, owner is `owner_id` not a row) lands now for the per-canvas **sharing UI in the NEXT prompt**; five SECURITY DEFINER helpers (`user_is_pro`, `canvas_is_pro`, `is_canvas_owner`, `can_access_canvas`, `can_edit_canvas`); the touch trigger now makes `project_id`/`owner_id` **immutable**; and the canvas RLS is rewritten so create/edit is **double Pro-gated** by the *right* plan (project canvas → board owner via `project_is_pro`; personal → owner via `user_is_pro`). Frontend: `/canvas` (`CanvasHome`) was rebuilt from a project **picker** into a real **full-width workspace** — the same `EntityPicker` + lazy `CanvasEditor` from P3.1b, listing **every** canvas labelled Personal vs its project, "+ New" → a personal canvas, Pro-gated via `<ProGate>` (Konva stays code-split: main bundle Konva-free, `CanvasEditor` chunk 324 kB/100 kB gz). `api.ts`/`useCanvas.ts` gained the aggregated list + personal-create + null-project save/delete; the per-project Canvas tab (`CanvasPanel`) is unchanged. typecheck + lint + build clean. ⚠️ One-time: apply the migration. **Earlier — Notes & Canvas full-width layout refactor:** markup/layout only; no data/RLS/autosave/Pro-gating change. Both the per-project **Notes** and **Canvas** tabs dropped the two-column `lg:grid-cols-[18rem_…]` side-list for a single **full-width** editor on every breakpoint, fronted by a new shared **glass dropdown picker** (`components/forms/EntityPicker.tsx`, on the `GlassSelect` `.glass-menu` surface — trigger shows the selected title + chevron; popover lists every item as title + subtitle [notes: snippet or "Empty note"; canvas: "Grid page · 2m ago"]; closes on select/outside/Escape; keyboard-navigable ↑/↓/Home/End/Enter; reduced-motion aware) next to the count + "+ New". A shared **`SegmentedToggle`** (`components/forms/SegmentedToggle.tsx`) powers both editor toggles. **NoteEditor:** Edit/Preview toggle now on **all** sizes (was `lg:hidden`), only the active pane renders full-width (removed `lg:grid-cols-2` + the `hidden lg:block` overrides), it grows to fill. **CanvasEditor:** the Konva stage now **fills** the tab (removed `h-[58vh] sm:h-[66vh]` + the padded GlassPanel wrapper → `relative min-h-[75vh] flex-1`, stage `absolute inset-0`); added an Edit/**View** toggle (editors only) — View renders the stage read-only (pan/zoom only) and hides add/undo-redo/lock/delete + disables the page-type switcher (kept as a read-only indicator) while keeping zoom, via a derived `editing = canEdit && mode==='edit'` passed to `CanvasStage`/`CanvasToolbar`/the keyboard handler. Removed the mobile back-arrow + `onBack` from both editors (→ `onDeleted`) and the swap-list logic; kept most-recent-default + delete→fall-back-to-next, loading/error/empty states, autosave + indicator + inline title + delete-confirm, and **all RLS/Pro gating**. Canvas stays lazy-loaded (main bundle unchanged; canvas chunk 329 kB/101 kB gz). typecheck + lint + build clean. Earlier: **Pro P3.1 — Notes Canvas foundation** — shipped the per-project **Notes Canvas** scaffold in migration `20260622180000_canvas.sql` (`canvas_notes`: `page_type` blank/ruled/grid/dotted, `scene jsonb` denormalised elements, `doc_state bytea` reserved for Yjs in P3.7, touch trigger stamping `updated_at`/`updated_by`; RLS read=`is_project_member`, delete=`can_edit_project`, **INSERT/UPDATE require `project_is_pro`** so free boards can't create/edit) + a **lazy-loaded `features/canvas` module**. A new **"Canvas" sidebar nav** → `/canvas` `CanvasHome` project picker (Pro-gated) and a **per-project Canvas tab** (`?tab=canvas`, `<ProGate isPro={isProBoard}>`). `CanvasStage` is an infinite pan/zoom **Konva** stage on the glass/aurora surface with a **world-space page-pattern background** (`PageBackground` draws ruled/grid/dotted that pans+zooms with content); selection + **Transformer** (move/resize/rotate); a **floating glass toolbar** (`CanvasToolbar`: undo/redo, add-element placeholder, page-type `GlassSelect`, zoom −/%/+, lock/delete-selected). **Element model** = a TS discriminated union `CanvasElement = Stroke | TextBox | Image | Media` (shared base id/x/y/width/height/rotation/z/locked) in `scene jsonb`, parsed defensively with Zod on read; P3.1 only creates a **TextBox stub** (real bodies arrive P3.2–P3.5; stub renderers for all four). **Undo/redo** = a swappable local snapshot command stack (`useSceneHistory`, reducer past/present/future — to be replaced by Y.UndoManager in P3.7). **Autosave** = debounced 700ms + flush-on-unmount writing `scene`/`page_type`/`title`, on `['canvas', noteId]` (full doc) + `['canvas-list', projectId]` (summaries) caches. Touch + stylus + mouse all pan (drag empty)/zoom (wheel + pinch)/select; keyboard undo-redo-delete. **Konva is fully code-split** — `CanvasPanel` is `React.lazy`'d from ProjectPage via a direct dynamic import (barrel exports only the Konva-free `CanvasHome`), so the canvas chunk (**330 kB / 101 kB gz**) loads only on canvas open; main bundle unchanged (~1086 kB). `proFeatures.canvas` → **shipped**. New files: `lib/canvasPages.ts`, `hooks/useElementSize.ts`, `features/canvas/{elements.ts, history.ts, schemas.ts, api.ts, useCanvas.ts, useCanvasPalette.ts, constants.ts, PageBackground.tsx, elementRenderers.tsx, CanvasStage.tsx, CanvasToolbar.tsx, CanvasEditor.tsx, CanvasPanel.tsx, CanvasHome.tsx, index.ts}`; `database.ts` gained `canvas_notes` + `CanvasNote`. typecheck + lint + build all clean. ⚠️ One-time: apply the migration (Next step). Earlier: **Pro P2 — collaboration** — shipped the full Pro collaboration layer in migration `20260622160000_collaboration_pro.sql` + a new `features/collaboration` module: **threaded comments** (`comments`, Pro-gated INSERT resolved from the card via `can_access_card`+`card_project_is_pro`) with **@mention autocomplete** (plain-text `@Name`, resolved to `comment_mentions` → notify trigger) and **edit/delete**; **emoji reactions** (`reactions`, polymorphic comment/card, Pro-gated) with an add-popover; a **card review flow** (`cards.review_status`/`review_assignee_id`/`reviewed_by`/`reviewed_at` + `ReviewControl`: request→approve/request-changes/clear) with a colored **review badge** on the card face + a **board-toolbar review filter**; an append-only **activity log** (`activity_log`, written only by SECURITY DEFINER triggers) shown per-project (new **Activity tab**) + per-card; and a **notification inbox** (`notifications`, own-row, trigger-written) powering a **Topbar bell** with a live unread count. All Pro-gated on the **board owner's** plan via a new `useProjectIsPro` + `<ProGate isPro>` override (RLS + `project_is_pro` are the real gate). Realtime: the 5 new tables joined `supabase_realtime` (REPLICA IDENTITY FULL); `useProjectRealtime` invalidates `['comments'/'activity'/'reactions']`, and `useNotifications` runs its own per-user channel. Optional **email** notifications reuse the existing `send-due-reminders` cron (a 3rd pass + 2 service-role RPCs, gated on `reminder_emails_enabled`; no new secret). `database.ts` + `supabase/README.md` + `plan.md` data model already listed `comments`. typecheck + lint + build clean (bundle 1076 kB / 307 kB gz). ⚠️ Apply the migration + redeploy the function — see Next step. Earlier same day: **Custom on-brand date+time picker** — replaced the native `<input type="date">`/`<input type="time">` with `components/forms/DatePicker.tsx` (glass month-grid + inline time selector, keyboard-navigable, both themes); `DueDateField` swaps it in with the **same value contract**. Audited: **no native date/time/select controls remain in `src/`**. Pure UI, no logic change; typecheck + lint + build clean. Earlier same day: **Pro P1 — custom timed reminders** shipped: `cards.due_at` + `card_reminders` + dispatch ledger + 2 service-role RPCs in migration `20260622140000_custom_reminders.sql`; the `send-due-reminders` Edge Function now runs both the free day-based digest and the precise email path; **pg_cron → every 10 min**; browser `useTimedReminders` fires `channel='push'` offsets; card modal gets a Pro time picker + `RemindersSection`; `due_at` threaded through board + calendar. Also extracted a reusable **`GlassSelect`** and converted the **last two native `<select>`s** (no native selects remain in `src/`). typecheck + lint + build clean. ⚠️ Apply the new migration + reschedule pg_cron. Earlier same day: **Switched the payment provider Stripe → Dodo Payments** — a Merchant of Record that localizes currency + remits sales tax/VAT. Stripe was never activated, so the swap is data-safe. New migration `20260622120000_dodo_billing.sql` (swaps `stripe_*`→`dodo_*` id columns on `profiles`, extends the `protect_plan_columns` guard; `plan`/`plan_status`/`current_plan()`/`project_is_pro()` unchanged); three new Edge Functions (`dodo-create-checkout`, `dodo-portal`, signature-verified `dodo-webhook`) replace + delete the three Stripe ones; `features/billing` (`api.ts`/`useBilling.ts`) calls the Dodo functions with the same `startCheckout(interval)`/`openPortal()` shapes; copy → "Secure checkout via Dodo Payments"; `database.ts`, `plans.ts`, `.env.example`, `supabase/README.md`, `plan.md` updated. typecheck + lint + build all clean. ⚠️ One-time setup pending — see Next step. Prior session: Pro P0 gating + storage foundation.)

---

## ✅ Done log

- [x] **Pro P3.7 — canvas live multiplayer (Yjs + Supabase Realtime + cursors) (2026-06-29).** Converted the canvas to a CRDT and made it collaborative. **Doc model (`collab/yCanvasDoc.ts`):** one `Y.Doc`/note — `elements: Y.Array<Y.Map>` (each Y.Map = one element's flat fields, same shape as the `scene` jsonb; array order is irrelevant since the snapshot sorts by `z`), one **top-level `Y.XmlFragment` named `text:<id>` per TextBox**, and a `meta: Y.Map` (`pageType`, now collaborative). Helpers: `buildDocFromScene` (seeds an empty doc from `scene` jsonb incl. converting each TextBox `body` JSON → fragment via `@tiptap/y-tiptap` `prosemirrorJSONToYXmlFragment`), `applySceneDiff` (turns a whole-scene `commit` into minimal add/delete/per-field-set ops in one `LOCAL_ORIGIN` transaction), `docToScene` (Y→plain snapshot, **dedupes by id** to survive a rare concurrent first-seed), `syncTextBody` (writes the edited body/text cache under `TEXT_SYNC_ORIGIN` so it stays off the element undo stack). **Hook (`collab/useYjsCanvas.ts`)** is a drop-in for the old `useSceneHistory`: same `{scene,commit,undo,redo,canUndo,canRedo}` + `pageType/setPageType` + `encodeStateHex` + `fragmentFor`/`syncTextBody`/`caretProvider` + `remotePeers`/`setLocalCursor`/`setLocalSelection`/`connection`. `commit = applySceneDiff`; undo/redo = a **`Y.UndoManager`** scoped `trackedOrigins:{LOCAL_ORIGIN}`, `captureTimeout:0` (one commit = one step) so it only reverts the local user's edits, never a collaborator's. The doc is created + seeded once per canvas (component keyed by note id), torn down on unmount. **Provider design (`collab/SupabaseYjsProvider.ts`) — novel for this repo, read before touching realtime:** a thin provider over **one private Supabase Realtime broadcast channel `canvas:<noteId>`** (`config.private=true`, `broadcast.self=false`). Wire events (payloads = base64 `Uint8Array`): `sync-step-1{sv}` (on (re)join, "here's my state vector"), `sync-step-2{update}` (reply with the diff the requester lacks, `Y.encodeStateAsUpdate(doc, theirSV)`), `update{update}` (live local Yjs update), `awareness{update}`. Remote updates are applied with the **provider instance as the transaction origin** so `doc.on('update')` can skip echoing them (no ping-pong). Awareness uses `y-protocols/awareness` (`encode/applyAwarenessUpdate`, `removeAwarenessStates` on destroy), **throttled ~50 ms**; doc updates send immediately (Yjs already coalesces a transaction into one update). A soft **200 KB payload cap** guards pathological cases (media bytes never enter the doc, so deltas are tiny). `setAuth()` is called before subscribe so the **private channel's RLS** (migration below) authorises every message. Reconnect: Supabase auto-reconnects the socket; on re-SUBSCRIBED we re-run the sync handshake + re-publish awareness (full DB re-seed happens on remount). **Cursors/halos (`RemotePresenceLayer.tsx`):** an HTML overlay (like `TextLayer`) drawing each peer's selection halo (coloured outline per selected element box) + live cursor (pointer + name pill + `Avatar`), world→screen via the same camera transform; cursor glide uses a CSS transition disabled under `prefers-reduced-motion`. **Text:** `richText.collabTextExtensions` swaps StarterKit's `undoRedo:false` + `Collaboration.configure({fragment})` + `CollaborationCaret.configure({provider, user})`; `RichTextBox` no longer takes `content` (the fragment is the source, pre-seeded from `body`) and mirrors edits back into the body/text cache (debounced). **Persistence:** `CanvasEditor`'s existing debounced autosave now also sends `doc_state = encodeStateHex()` whenever the scene changed; `api.patchCanvas`/`useSaveCanvas` accept `doc_state` (BYTEA written as a `\x…` hex literal, read back as hex). **doc_state column usage:** authoritative Yjs snapshot — on open, if `doc_state` is present the doc seeds from it (fragments + elements intact); else it seeds from `scene` jsonb (first realtime session only). `scene` jsonb stays the denormalised fast-read/thumbnail copy. **Gating:** join only for a Pro canvas (project canvas → board owner via `useProjectIsPro`; personal → owner via `useIsPro`, shared members join too); the **real gate is RLS** on the channel. **Migration `20260629120000_canvas_realtime.sql`:** two `realtime.messages` policies — receive (select) requires `can_access_canvas(noteId)`, send (insert) requires `can_edit_canvas(noteId)`, with a CASE-guarded uuid cast so only `canvas:` topics are checked; PUBLIC channels (presence/notifications) are unaffected. New files: `features/canvas/collab/{yCanvasDoc, SupabaseYjsProvider, useYjsCanvas, awareness, encoding}.ts` + `RemotePresenceLayer.tsx`; touched `CanvasEditor/CanvasStage/TextLayer/RichTextBox/richText/api/useCanvas`; **`history.ts` superseded** by `useYjsCanvas` (now unused — `git rm` it; left on disk only because the in-session mount can't delete). Deps already present (`yjs`, `y-protocols`, `@tiptap/extension-collaboration`, `-collaboration-caret`, `@tiptap/y-tiptap`). typecheck + eslint + `vite build` clean (verified in a clean reconstructed tree; canvas chunk stays lazy + separate, 930 kB/287 kB gz). **Bugfix (post-deploy):** editing a text box black-screened the app — `collaboration-caret` reserves the awareness fields **`user`** and **`cursor`** for the in-text caret (a ProseMirror position), but the canvas presence was writing the pointer `{x,y}`/`null` into `cursor`, so the caret plugin read it as a text position and threw `Cannot read properties of undefined (reading 'type')`. Fixed by namespacing the canvas presence onto `canvasUser`/`canvasCursor`/`canvasSelection` (`awareness.ts` constants + `useYjsCanvas` writers), leaving the caret's `user`/`cursor` fields untouched. **⚠️ One-time: apply `20260629120000_canvas_realtime.sql`. ⚠️ Build + commit pending on the user's machine** (the in-session mount truncates working-tree reads — see Status).

- [x] **Pro P3.6 — canvas element management, build fixed + lint clean (2026-06-29).** Pin/lock (per-element, blocks select/move/resize), visibility toggle, z-order (bring forward/backward, to front/back), multi-select via marquee + Shift+click with group move, duplicate, delete, copy/paste, a right-click + long-press (600 ms) glass context menu (`ContextMenu.tsx`), a layers panel (`LayersPanel.tsx`), and full keyboard shortcuts (Del, Ctrl/⌘+C/V/D/Z, Shift+Z, `[`/`]` ±Shift z-order, arrow nudge ±Shift). Keyboard + touch + stylus parity preserved; earlier sub-phases (draw/erase, text, image, media) untouched. Resolved the Cloudflare build failure (follow-up commits `90b215d`/`fa3ebab`/`09bd1c7`) and cleared 5 residual ESLint errors (ternary-as-statement → if/else in `CanvasEditor.tsx`; dropped unnecessary `as Konva.Node` assertions in `CanvasStage.tsx`). typecheck + `eslint src` + `vite build` all clean (built from git objects in a clean dir due to the truncating sandbox mount). No DB migration.

- [x] **Pro P3.5 — canvas audio + video (2026-06-29).** Both in-app record/upload AND allow-listed embeds. **Record/upload:** `Clapperboard` toolbar button → lazy `AddMediaModal` (Record/Upload/Embed). `useMediaRecorder.ts` wraps `MediaRecorder` (audio webm/opus, video webm/vp9 + fallbacks), permission UX, duration, size meter vs cap, auto-stop at cap. Validate (`MEDIA_CAPS` audio 25 MB / video 100 MB) → `uploadCanvasMedia` → `MediaElement` (`source:'file'`, storage `path`). **Embed:** `embeds.ts` allow-list (YouTube/Vimeo/Loom/SoundCloud) parses + rebuilds the canonical embed URL (rejects non-http(s)/unknown hosts; never the raw string) → `MediaElement` (`source:'embed'`, `embedUrl`). **Render:** HTML overlay `MediaLayer` (mirrors `TextLayer`) — `<audio>`/`<video>` via shared `useSignedUrl.ts` for files, responsive `<iframe>` (strict referrerPolicy, lazy, canonical src only) for embeds; Konva draws only the bg rect. Interactive when selected (edit) or always in View/viewer; click-through otherwise so Konva keeps select/drag; Transformer `padding` keeps handles grabbable; video keeps aspect ratio. Drag-drop routes audio/video. `<ProGate>` UX + Storage RLS real gate; personal canvases excluded; no DB migration. New: `features/canvas/{embeds.ts, MediaLayer.tsx, useMediaRecorder.ts, AddMediaModal.tsx, useSignedUrl.ts}`; touched `elements.ts, elementRenderers.tsx, CanvasStage.tsx, CanvasToolbar.tsx, CanvasEditor.tsx, lib/proFeatures.ts`. **Built + committed + pushed + verified live (commit `5914c2c`).** No migration.

- [x] **Pro P3.4 — canvas image elements (2026-06-29).** File picker (`ImagePlus` toolbar button) + clipboard paste + drag-drop onto the canvas. Validates type+size against `MEDIA_CAPS.image` (PNG/JPEG/GIF/WebP, 10 MB cap) client-side; friendly dismissible error banner on oversize/wrong-type. Uploads to `canvas-media` bucket via `uploadCanvasMedia` (path `<projectId>/<noteId>/<uuid>.<ext>`); stores an `ImageElement` with a `path` field — never a data URL. Renders via a **module-level signed URL cache** (`urlCache`+`inFlight` Maps, deduplication + 1-hour validity) + manual `new window.Image()` load in `useEffect` (no `use-image` dep). Skeleton while loading; error fallback if unavailable. Move/resize (**keepRatio:true** by default on the Transformer; Shift frees it), rotate, lock, z-order, undo/redo all work via the existing element infrastructure. Personal canvases excluded (Storage RLS only covers project canvases). Paste guarded by `editingTextId !== null` check so Tiptap text boxes still receive paste events normally. `proFeatures.media.shipped = true`. Files touched: `lib/proFeatures.ts`, `features/canvas/{elements.ts, elementRenderers.tsx, CanvasStage.tsx, CanvasToolbar.tsx, CanvasEditor.tsx}`. typecheck clean; no migration.

- [x] **Pro P3.3 — canvas rich text finished to spec (2026-06-29).** Added text colour (`TextStyle`+`Color`) and XSS-safe inline links (hardened `SafeLink`, href sanitised on parse+render via a shared `safeLinkHref` http/https/mailto allow-list) + toolbar controls for both. New dep `@tiptap/extension-link`. Verified: DOM-shim render test rejects `javascript:`/`data:` hrefs, keeps https/mailto; typecheck + lint + build clean.

- [x] Requirements gathered (sync ✔, collaborators ✔, features ✔, free + sellable ✔).
- [x] Architecture & stack chosen — see `plan.md` §3.
- [x] Aurora design system defined — see `plan.md` §4.
- [x] Data model & security model drafted — see `plan.md` §5–6.
- [x] Roadmap + per-phase build prompts written — see `prompt.md`.
- [x] Foundation docs created: `CLAUDE.md`, `plan.md`, `memory.md`, `prompt.md`, `prompts.html`, `SETUP.md`.
- [x] Environment ready: GitHub repo, Supabase, Cloudflare, Node.js. Repo wired into Phase 0.
- [x] **Phase 0 — Setup & infra** — Vite + React + TS (strict) scaffolded in place; Tailwind/PostCSS, Framer Motion, dnd-kit, TanStack Query, React Router, supabase-js, Zod, date-fns, vite-plugin-pwa installed & wired; typed Supabase client; ESLint (flat) + Prettier; clean folder structure; README run/deploy docs; git initialized + first commit + pushed to GitHub.
- [x] **Phase 1 — Design system & app shell** — Full Aurora theme (CSS-var tokens for both first-class light/dark, six accent gradients, glass/semantic values), self-hosted fonts (Space Grotesk + Inter via @fontsource-variable), animated `AuroraBackground` (drifting blobs + pointer parallax + grain), flowing-gradient text/buttons + rotating conic gradient-border, and the core component library: `GlassPanel`, `GlassCard` (3D pointer tilt), `GradientButton` (3D tactile, variants/sizes), `Field`, `Badge`, `Avatar`, `Modal`, `Spinner`, `Skeleton`, `ThemeToggle` (persisted, View-Transitions cross-fade), `Reveal` motion wrapper. Responsive app shell (collapsible glass sidebar / mobile drawer + bottom nav + top bar). Routes: `/` (Boards), `/calendar`, `/notes` (placeholders), `/style-guide` showcase. All heavy motion gated behind `prefers-reduced-motion`.
- [x] **Phase 2 — Auth (email/password + Google)** — `profiles` table (1:1 with `auth.users`) + `handle_new_user()` SECURITY DEFINER trigger (auto-creates a profile on sign-up) + own-row RLS, as a migration under `supabase/migrations`. Aurora-styled **Sign Up / Log In / Forgot Password** screens (shared `AuthLayout` glass card on the aurora bg) plus a **Continue with Google** button and a **Set-new-password** screen for the recovery link; all inputs validated with Zod, friendly inline + form-level errors. `AuthProvider` + `useAuth` expose session/user/loading (Supabase `onAuthStateChange` + persistence/refresh); `useProfile`/`useUpdateProfile` (TanStack Query) back the nav + profile screen. `ProtectedRoute` / `PublicOnlyRoute` guards; a `UserMenu` (avatar → Profile / Sign out) replaced the placeholder avatar in the top bar. Minimal **Profile** screen edits the display name.
- [x] **Phase 3 — Projects (workspaces) + multi-tenant RLS** — `projects` (`owner_id`, `name`, `description`, `accent`, `created_at`) + `project_members` (`role` owner/editor/viewer, PK `project_id`+`user_id`) migration. An `after insert` trigger (`handle_new_project()`, SECURITY DEFINER) seeds the creator as an `owner` member. RLS: projects readable by members (`is_project_member(id)`), insertable for self, update/delete owner-only; members readable by fellow members, insert/delete owner-only (`is_project_owner(project_id)`). Both checks run through **SECURITY DEFINER** helpers so policies never sub-query `project_members` directly → no recursion. Frontend: a `features/projects` module — `ProjectsPage` dashboard (vivid Aurora glass `ProjectCard`s with accent gradients, stretched-link nav, owner-only edit/delete), `ProjectFormModal` (name + description `TextArea` + six-option `AccentPicker`), `DeleteProjectDialog`, `ProjectPage` route (accent-themed, board placeholder). Data via **TanStack Query** (`useProjects`/`useProject` + create/update/delete with optimistic cache writes), validated with **Zod**. New shared `TextArea` form primitive. `ProjectsPage` is now the index route; the old sample `Home` page was removed.
- [x] **Phase 4 — Kanban board** — `columns` (`project_id`, `name`, `position`, `created_at`) + `cards` (`project_id`, `column_id`, `title`, `description`, `due_date`, `assignee_id`, `position`, `created_at`) migration. RLS on both: every op gated on `is_project_member(project_id)` (the Phase 3 SECURITY DEFINER helper — no new recursion work). A `seed_project_columns()` SECURITY DEFINER `after insert` trigger gives new projects To Do/In Progress/Done; a one-time backfill seeds existing projects. Frontend `features/board` module: `Board` (DndContext + DragOverlay + confetti orchestrator), `BoardColumn` (glass panel, draggable grip, inline rename, delete dialog, quick-add composer), `BoardCard`/`CardSurface` (glass card with accent glow), `CardDetailModal` (title + description now; checklists/due/labels/assignee reserved for Phase 5), `DeleteColumnDialog`, `AddColumn`, `Confetti`. Ordering via fractional `position` midpoints (`ordering.ts` — `positionBetween`/`neighbourPosition`, no collisions). Data on a single `['board', projectId]` TanStack cache with optimistic add/rename/delete/move for columns and cards (`useBoard.ts`). dnd-kit: Mouse(distance) + Touch(press-delay) + Keyboard sensors, `closestCorners`, live cross-column preview in `onDragOver`, columns + within-column reflow via applied sortable transforms; confetti fires only when a card *enters* a Done-type column. Zod-validated inputs; all heavy motion behind reduced-motion.
- [x] **Phase 5 — Card details (to-dos, due dates, labels)** — `checklist_items` (id, card_id, text, is_done, position) + `labels` (id, project_id, name, color) + `card_labels` (card_id, label_id, composite PK) migration. RLS: `labels` gated on `is_project_member(project_id)`; `checklist_items` + `card_labels` gated on a new SECURITY DEFINER helper `can_access_card(card_id)` that resolves the card's project once (keeps policies flat, no cross-table RLS re-entry). Label `color` is a name constrained by a CHECK (like `projects.accent`), mapped to a hex in `src/lib/labelColors.ts`. Frontend (extends `features/board`): one optimistic `['card-extras', projectId]` cache ({labels, checklist, cardLabels}) via `cardExtras.api.ts` + `useCardExtras.ts`; `due.ts` (urgency status/label helpers, date-fns); rebuilt `CardDetailModal` with `DueDateField`, `CardLabelsSection` (attach/detach + inline create/delete with a swatch picker), and `Checklist` (`ChecklistItemRow`, nested dnd-kit reorder, progress bar); `LabelPill` presentational; `CardSurface` now shows label swatches, a urgency-colored due pill, and a "done/total" tally; `BoardToolbar` (title search + due chips + label chips) with `display:none` filtering so drag ordering stays correct. Card due date persists via the extended `useUpdateCard` (now writes `due_date`). typecheck/lint/build clean.
- [x] **Phase 6 — Calendar view** — `features/calendar` module. **No new migration** (reuses `cards.due_date`). `api.ts`: `fetchDatedCards()` (one query, `due_date is not null`, RLS-scoped across every project) + `updateCardDueDate()`. `useCalendar.ts`: a `['calendar-cards']` query plus `useRescheduleCard` / `useUpdateCalendarCard` / `useDeleteCalendarCard` — each optimistic and **cross-cache** (patches the project's `['board', id]` and, on delete, `['card-extras', id]` too, so changes show instantly on return to the board). `dates.ts`: date-fns helpers (month/week day arrays, `toDateKey` local `YYYY-MM-DD`, `periodLabel`, `groupCardsByDate`). UI: `CalendarPage` (DndContext orchestrator, view/scope/cursor state, both modals), `CalendarToolbar` (title + period, month/week segmented toggle, prev/today/next, project scope select), `CalendarGrid` → `DayCell` (droppable day, accent today-pill, "+N more"), `CardChip`/`DraggableCardChip` (accent bar + urgency dot; presentational chip backs grid/overlay/agenda/peek), `AgendaList` (mobile), `DayCardsModal` (day overflow). New shared hook `src/hooks/useMediaQuery.ts`. Calendar route wired in `App.tsx` (replaced the placeholder). typecheck/lint/build clean.
- [x] **Post-Phase-6 batch (2026-06-20, user-requested)** — **(1) Bugfix:** the card modal couldn't add checklist items or labels — both composers were `<form>`s **nested inside the modal's own `<form>`** (invalid HTML), so clicking "+" bubbled up and submitted/closed the card (looked like a reload). Converted both to plain inputs with explicit click + Enter handlers (`Checklist.tsx`, `CardLabelsSection.tsx`). **(2) 3D buttons read flat at rest** — deepened the resting/hover/active shadow stacks in `.btn-3d`/`.btn-3d-soft` and increased the lift travel (`index.css`, `GradientButton.tsx`). **(3) Task priority** — open-ended integer `cards.priority` (P1 = highest, no upper bound), `PriorityField` picker (P1–P10 quick + "Higher…" custom), tier-colored `P#` pill on the card face; flows through the shared `updateCardDetail`, so the board **and** calendar persist it (`lib/priority.ts` + migration `20260620100000_card_priority.sql`). **(4) Daily to-do planner** — new `features/todos` module + sidebar **To-Do** tab: per-day, **multiple named lists** (Personal/Work…), each its own checklist (add/tick/delete, rename/delete list), day navigation; **private to the user** via own-row RLS + an `owns_todo_list()` SECURITY DEFINER helper (migration `20260620120000_todos.sql`). Verified by inspection + dev-machine `npm run typecheck` (the sandbox file-mirror desynced, so in-session `tsc` was unreliable — same flaky-mount issue as the 2026-06-18 incident).
- [x] **Phase 7 — Notes/docs (per project)** — `notes` table (id, project_id, title, content, updated_at, created_at) + membership-gated RLS (`is_project_member(project_id)`) + a `before update` trigger stamping `updated_at` server-side (migration `20260620140000_notes.sql`). New `features/notes` module: `NotesPanel` (two-pane list + editor, responsive list↔editor swap), `NoteEditor` (inline title rename, markdown textarea + **live preview** side-by-side/toggle, inline-confirm delete, **debounced 700 ms autosave** with flush-on-unmount + a subtle saved indicator), `markdown.tsx` (self-contained, XSS-safe markdown→React renderer — headings/bold/italic/code/fences/lists/quotes/hr/links, no dep, no `dangerouslySetInnerHTML`), `useNotes`/`api`/`schemas`, and `NotesHome` (the global `/notes` route → a project picker deep-linking to each project's Notes tab). Wired as a `?tab=notes` Board/Notes tab on `ProjectPage`. typecheck/lint/build clean.
- [x] **Phase 8 — Collaboration** — `invitations` table (email-keyed, editor/viewer) + redemption (`redeem_invitations_for`/`redeem_my_invitations` RPC + an `auth.users` signup trigger that auto-joins new users). Role-aware RLS: reads stay membership-gated; writes on `columns`/`cards`/`checklist_items`/`labels`/`card_labels`/`notes` now require owner/editor via new `can_edit_project()`/`can_edit_card()` SECURITY DEFINER helpers (viewers read-only at the DB). `project_members` gains an owner-only role-UPDATE policy; a `protect_project_owner_membership` trigger keeps the owner row from being demoted/removed/duplicated. `profiles` gains a co-member SELECT policy (`shares_a_project_with()`) so members see each other's avatars/names. Realtime: content tables + `project_members` added to the `supabase_realtime` publication with `REPLICA IDENTITY FULL` (migration `20260620160000_collaboration.sql`). New `features/members` module — `api`/`useMembers`/`schemas`, `MembersBar` (header avatar stack + presence), `MembersPanel` (roster/roles/invite/pending), `RoleControl`, `useProjectRealtime` (debounced cache invalidation), `usePresence`, `useRedeemInvitations`. Board/CardDetailModal/BoardColumn/NotesPanel/NoteEditor + the calendar card modal thread a `canEdit` (read-only) prop. typecheck/lint/build clean.
- [x] **Phase 9 — PWA & reminders** — Installable PWA: full web manifest (icons + **maskable** + shortcuts + id/scope/start_url), Workbox SW (`registerType: 'prompt'`) precaching the app shell with SPA `navigateFallback`; Aurora icons generated by a **zero-dep** `scripts/generate-icons.mjs` (hand-rolled PNG encoder); dynamic light/dark `theme-color` via `applyTheme()`. Offline UX: localStorage-persisted TanStack Query cache (read-only cached view, wiped on sign-out), `useOnlineStatus` → `OfflineBanner`, and a `PWAReloadPrompt` ("ready offline" + one-tap update). Reminders (**Both**): a secret-gated Deno Edge Function `send-due-reminders` (Resend digests to assignees, `due_reminder_candidates`/`mark_reminders_sent` RPCs) on a pg_cron schedule, **and** zero-setup browser notifications (`features/reminders` — `useDueReminders` + `ReminderSettings` on Profile), backed by new `profiles.reminder_emails_enabled`/`reminder_lead_days`. Added a card `AssigneeField` so reminders have a target (`assignee_id` now persists via the card modal). Migration `20260621090000_reminders.sql`. typecheck/lint/build clean.
- [x] **Phase 10 — Polish & launch (built in code 2026-06-21; pending Stripe setup + build/push)** — Public **marketing site** (`features/marketing`: `MarketingLayout`, animated `HeroSection` + `FauxBoard`/`FauxCalendar`/`FauxCardDetail`/`FauxNotes` previews, features, showcase, testimonials, pricing) at `/`, `/pricing`, `/terms`, `/privacy`. **Stripe billing** (Free vs Pro $8/mo): `features/billing` (`BillingPage`, `UpgradeModal`, `PlanBadge`, `useBilling`) + 3 Edge Functions (`create-checkout-session`, `create-portal-session`, signature-verified `stripe-webhook`). **Plan limits** server-enforced: `src/lib/plans.ts` (Free = 3 projects) + a `projects` insert trigger + a `profiles` billing-column-protection trigger + `current_plan()` (migration `20260621210000_billing.sql`). **Routing split:** `/` public landing, dashboard moved to `/boards`; new `/billing` route + UserMenu link + a `PlanBadge`. ProjectsPage shows `n/3` usage + an `UpgradeModal` at the limit. *Built by Claude + 2 subagents; pending `npm run build`, the Stripe dashboard/secret setup (supabase/README → Stripe billing), and commit/push.*
- [x] **Post-launch polish (2026-06-21)** — Email reminders fully set up + verified end-to-end (Resend key + `send-due-reminders` deployed `--no-verify-jwt` + secrets + pg_cron; a real reminder email landed). UI fixes: native `<select>`s can't be styled, so the **role + assignee pickers became custom dropdowns** on a new opaque **`.glass-menu`** surface (also used by the profile menu, which was see-through); the sidebar **"New project"** button (a no-op) now navigates `?new=1`; **offline detection** now probes Supabase (`navigator.onLine` lied on Windows) so the **OfflineBanner** actually fires; the **card modal** scrim is frostier. Reminders Edge Function bugfix: don't `JSON.parse` a void RPC's empty body. **Invitation-flow redesign:** invites no longer auto-join — the invitee **accepts/declines** (a `PendingInvitations` banner on Boards + `useInvitations` hooks + `accept_invitation`/`my_pending_invitations` RPCs) and any member can **leave** a project; dropped the signup auto-redeem trigger (migration `20260621180000_invitation_accept.sql`, applied 2026-06-21). Built + pushed 2026-06-21 (`b02d44b`, `npm run build` clean).
- [x] **Phase 10.1 — collab limits, feedback, CEO message, iOS safe-area (built in code 2026-06-21; pending migration + build/push)** — **Responsive/iOS:** the app shell respects `env(safe-area-inset-*)` (container top/sides, `main` bottom, fixed `BottomNav`, mobile drawer) so content clears the notch / Dynamic Island / home indicator on all devices. **Bigger free limits:** `FREE_PROJECT_LIMIT` 3→**10** + a new `FREE_MEMBER_LIMIT` = **3 members/board** (the board owner's plan governs), both enforced by triggers (`enforce_project_limit` recreated + new `enforce_member_limit`); `MembersPanel` shows an upgrade gate at the cap, `PendingInvitations` surfaces a friendly "board is full" message. **Feedback:** `feedback` table (RLS: submit/read own; admin reads all) + `features/feedback` (`FeedbackModal` in the user menu for everyone; admin-only `FeedbackInbox` tab). **CEO message:** `ceo_messages` table (everyone reads, admin writes) + `features/announcements` (`CeoMessagePage` at `/from-the-ceo` with an inline admin editor). Admin = a fixed email via `is_admin()` (DB, in RLS) + `src/lib/admin.ts` (UI gating). Subtle **"Made by J. Jai Akash"** in the sidebar footer. Migration `20260621230000_collab_limits_feedback_ceo.sql`. Feedback/CEO frontends built by a subagent against the locked schema/contracts.
- [x] **UX pass — Feedback/Features tab + mobile search polish (2026-06-21).** Surfaced feedback for **all** users: new shared `FeedbackForm` + `FeedbackPage` (form for everyone, inbox below for admin), `/feedback` route no longer admin-only, sidebar nav item now visible to all and relabeled **"Feedback/Features"** (`MessageSquareHeart`). Fixed the cramped mobile **top search bar** (`Topbar` affordance + `BoardToolbar` input `h-10`→`h-11` + `leading-none`). New files: `src/features/feedback/FeedbackForm.tsx`, `src/features/feedback/FeedbackPage.tsx` (barrel updated). Touched: `FeedbackModal.tsx` (now wraps `FeedbackForm`), `App.tsx`, `navItems.ts`, `Topbar.tsx`, `BoardToolbar.tsx`. *⚠️ Build/commit pending on a stable machine — sandbox mount corruption this session.*
- [x] **Pro pricing — $5.99/mo + annual (5% off) (2026-06-21).** `src/lib/plans.ts` ($5.99, `priceAnnual` $68.29, `PRO_ANNUAL_DISCOUNT_PCT`, helpers, `BillingInterval`); new `IntervalToggle`; `interval` threaded through `useBilling`/`api`/`create-checkout-session` (selects `STRIPE_PRICE_PRO` vs new `STRIPE_PRICE_PRO_ANNUAL`, with monthly fallback); UpgradeModal + BillingPage upsell + marketing PlanCard surface the annual option; `.env.example`/supabase README updated. *⚠️ Build/commit + create the yearly Stripe price pending on a stable machine.*
- [x] **Pro P0 — gating + storage foundation (2026-06-22).** Migration `20260622000000_pro_foundation.sql`: `project_is_pro(project)` SECURITY DEFINER helper (board owner's plan governs) + a **private** `canvas-media` Storage bucket + `storage.objects` RLS (member SELECT; member **AND** `project_is_pro` for INSERT/UPDATE/DELETE; projectId parsed from the path's first segment). Frontend: `src/features/billing/useIsPro.ts` (`useIsPro()`), `ProGate.tsx` (`<ProGate>` → upgrade CTA opening `UpgradeModal`), both in the billing barrel; `src/lib/proFeatures.ts` (single source of Pro capabilities + media caps + bucket name; `plans.ts` now pulls shipped Pro labels from it); `src/lib/storage.ts` (`uploadCanvasMedia`/`signedUrl`, validating type+size vs caps with a typed `MediaUploadError` before upload). `project_is_pro` added to `database.ts`. typecheck + build clean; new files lint-clean. Foundation only — no end-user Pro feature; **unblocks P1–P3** (prompts.md). ⚠️ Apply the migration in the Supabase SQL Editor (one-time).
- [x] **Custom date+time picker — no native OS controls left (2026-06-22).** New `src/components/forms/DatePicker.tsx`: on-brand glass month-grid picker (date-fns), prev/next-month, Today/Clear, optional inline time selector (three `GlassSelect`s ↔ `HH:mm`), full keyboard nav (arrows/PageUp·Down/Enter/Esc), reduced-motion aware, both themes. Expands **in-flow** (a glass disclosure) to avoid clipping inside the card modal's scroll box; Escape uses a capture-phase listener so it doesn't also close the modal. `DueDateField` now renders it (same `value`/`time`/`onChange`/`onTimeChange` contract — no other changes). Added `formatClockTime()` to `lib/dueAt.ts`. **Audited: no native `<input type="date|time|color|…">` and no native `<select>` remain in `src/`.** Pure UI — reminders logic + data contract untouched. typecheck + lint + build clean.
- [x] **Pro P1 — custom timed reminders (2026-06-22).** Migration `20260622140000_custom_reminders.sql`: `cards.due_at timestamptz` (source of truth when present; backfilled from `due_date` @09:00 UTC), `card_reminders` (per-card offsets + channel; RLS INSERT gated by new `card_project_is_pro()` so only Pro boards can create them), `card_reminder_dispatches` dedupe ledger (RLS-on/no-policies = server-only), and service-role-only RPCs `due_time_reminder_candidates(window)` / `mark_time_reminders_sent(ids)`. Edge Function `send-due-reminders` now runs **both** the free day-based digest (self-deduping) **and** the precise `channel='email'` path; **pg_cron rescheduled daily → every 10 min** (supabase/README). Browser: `useTimedReminders` (mounted in `useDueReminders`) fires `channel='push'` offsets once per `(reminder, due_at)`. UI: `DueDateField` time picker (Pro), `RemindersSection` (ProGate'd chips + custom + channel), `due_at` threaded through board + calendar caches/mutations. **Dropdown fix:** new reusable `GlassSelect` + converted the last two native `<select>`s (ReminderSettings, CalendarToolbar) — none remain in `src/`. `proFeatures.customReminders` → shipped; Pro plan lists it. New files: `lib/dueAt.ts`, `components/forms/GlassSelect.tsx`, `features/reminders/{offsets.ts, cardReminders.api.ts, useCardReminders.ts, RemindersSection.tsx, useTimedReminders.ts}`. typecheck + lint + build clean. ⚠️ One-time: apply the migration + reschedule pg_cron.
- [x] **Payments: Stripe → Dodo Payments (2026-06-22).** Replaced the (never-activated) Stripe billing with **Dodo Payments**, a **Merchant of Record** that localizes currency + remits sales tax/VAT. Migration `20260622120000_dodo_billing.sql`: drops `profiles.stripe_customer_id`/`stripe_subscription_id`, adds `dodo_customer_id`/`dodo_subscription_id`, and rewrites the `protect_plan_columns` trigger to guard `plan`/`plan_status`/`dodo_*` (service-role-only writes); `current_plan()` / `project_is_pro()` / the limit triggers untouched. Three new Deno Edge Functions calling the Dodo REST API directly (Bearer key, base `https://test.dodopayments.com` / `https://live.dodopayments.com` via `DODO_PAYMENTS_ENVIRONMENT`): **`dodo-create-checkout`** (authed; `POST /checkouts` with `product_cart` = monthly/annual product from env, `metadata.user_id`, `return_url`; returns `checkout_url`), **`dodo-portal`** (authed; `POST /customers/{id}/customer-portal/session` → `link`), **`dodo-webhook`** (`--no-verify-jwt`; verifies the **Standard Webhooks** HMAC-SHA256 signature over the raw body **before any write**, then maps subscription events → plan). The three Stripe functions were deleted. Frontend `features/billing` `api.ts`/`useBilling.ts` now invoke the Dodo functions, same `startCheckout(interval)`/`openPortal()` shapes; copy → "Secure checkout via Dodo Payments"; `database.ts`/`plans.ts` comments + columns updated. `.env.example`, `supabase/README.md` ("Dodo Payments billing"), and `plan.md` (§2/§3/§6/§7/§8) updated. typecheck + lint + build clean. ⚠️ One-time setup (apply migration; create the 2 Dodo test products; deploy 3 functions; add webhook endpoint + copy secret; set 5 secrets) in the Next step / README.
- [x] **Pro P2 — collaboration (2026-06-22).** Migration `20260622160000_collaboration_pro.sql` + new `features/collaboration` module. **Tables:** `comments` (project_id denormalised + kept consistent by a `comments_set_project` BEFORE-INSERT trigger; self-FK threads; `card_id`/`canvas_note_id` with a one-target CHECK — canvas is P3, no FK yet), `comment_mentions` (PK comment+user), `reactions` (polymorphic `target_type∈{comment,card}`, unique per target+user+emoji), `activity_log` (append-only), `notifications` (own-row inbox, `emailed_at` dedupe). **cards** gained `review_status`(`none`/`in_review`/`approved`/`changes_requested`)/`review_assignee_id`/`reviewed_by`/`reviewed_at`. **RLS:** read = `is_project_member`; comments + reactions **INSERT require Pro** (`card_project_is_pro`/`project_is_pro` — the real gate, resolved from the card not the client's project_id); `activity_log`/`notifications` have **no client INSERT policy** — written only by SECURITY DEFINER triggers (`comments_after_insert` → activity + reply-notify; `comment_mentions_after_insert` → mention-notify; `cards_review_after_update` → activity + review-notify; via a shared `notify()` that skips self). New definer helpers `comment_project_id`/`comment_author_id`/`target_project_id`. **Realtime:** the 5 tables joined `supabase_realtime` (REPLICA IDENTITY FULL); `useProjectRealtime` now invalidates `['comments',pid]`/`['activity',pid]`/`['reactions']`, and `useNotifications` runs its own per-user channel. **Frontend:** `CommentThread` (2-level threads, ProGate'd) + `CommentComposer` (@mention autocomplete over the roster, ↑/↓/Enter) + `CommentItem` + `MentionText`; `ReactionBar`; `ReviewControl` + `ReviewBadge` (card-face pill via `CardSurface`, board-toolbar `ReviewFilter`); `ActivityFeed` (project tab + per-card); `NotificationBell` (Topbar, live unread badge). Card modal shows review + reactions + comments + per-card activity in both editable & read-only views. Pro-gating is **board-owner-scoped** via new `useProjectIsPro` + a `<ProGate isPro>` override. New board hook `useUpdateCardReview` (+ `updateCardReview` api). **Email** (optional): `send-due-reminders` gained a 3rd pass emailing un-emailed notifications (RPCs `notification_email_candidates`/`mark_notifications_emailed`, gated on `reminder_emails_enabled`; no new secret). `database.ts` types extended (`ReviewStatus`/`ReactionTarget`/`NotificationKind` + 5 tables + row aliases). New files: `features/collaboration/{schemas.ts, mentions.ts, comments.api.ts, reactions.api.ts, activity.api.ts, notifications.api.ts, useComments.ts, useReactions.ts, useActivity.ts, useNotifications.ts, useProjectIsPro.ts, MentionText.tsx, ReactionBar.tsx, CommentComposer.tsx, CommentItem.tsx, CommentThread.tsx, ReviewBadge.tsx, ReviewControl.tsx, ActivityFeed.tsx, NotificationBell.tsx, index.ts}`. typecheck + lint + build clean. ⚠️ One-time: apply the migration + (for email) redeploy `send-due-reminders` — see Next step / README "Pro collaboration".
- [x] **Pro P3.1 — Notes Canvas foundation (2026-06-22).** Migration `20260622180000_canvas.sql`: `canvas_notes` (id, project_id, title, **page_type** ∈ blank/ruled/grid/dotted, **scene jsonb** = denormalised elements, **doc_state bytea** reserved for the Yjs snapshot in P3.7, updated_by/updated_at via a `touch_canvas_notes` BEFORE-UPDATE trigger). **RLS**: read = `is_project_member`, delete = `can_edit_project`, **INSERT/UPDATE require `project_is_pro`** (board owner's plan governs) so free boards can't create/edit a canvas even via the raw API. New **lazy-loaded `features/canvas`** module: a **"Canvas" sidebar nav** → `/canvas` `CanvasHome` picker (Pro-gated via `<ProGate>`) + a per-project **Canvas tab** (`?tab=canvas`, gated by `<ProGate isPro={isProBoard}>`). `CanvasStage` = an infinite **Konva** pan/zoom stage on the glass surface; `PageBackground` draws the page pattern in **world space** so it pans+zooms with content; selection + **Transformer** (move/resize/rotate); `CanvasToolbar` (floating glass: undo/redo, add placeholder, page-type `GlassSelect`, zoom −/%/+, lock/delete-selected). **Element model** = a TS discriminated union `CanvasElement = Stroke | TextBox | Image | Media` (`elements.ts`, shared transform base) stored in `scene`, Zod-parsed on read; only a **TextBox stub** is created in P3.1 (renderers for all four are stubs). **Undo/redo** = a swappable snapshot command stack (`useSceneHistory`, reducer-based — API kept stable for the P3.7 Y.UndoManager swap). **Autosave** = debounced 700ms + flush-on-unmount of `scene`/`page_type`/`title`, on `['canvas', noteId]` + `['canvas-list', projectId]` caches (`useCanvas.ts`). Touch + stylus + mouse pan (drag empty)/zoom (wheel + pinch)/select; keyboard undo/redo/delete. **Konva fully code-split**: `CanvasPanel` is `React.lazy`'d from `ProjectPage` via a direct dynamic import (barrel exports only the Konva-free `CanvasHome`) → a separate **330 kB / 101 kB gz** chunk; main bundle unchanged. `proFeatures.canvas.shipped = true`. New files: `lib/canvasPages.ts`, `hooks/useElementSize.ts`, `features/canvas/{elements.ts, history.ts, schemas.ts, api.ts, useCanvas.ts, useCanvasPalette.ts, constants.ts, PageBackground.tsx, elementRenderers.tsx, CanvasStage.tsx, CanvasToolbar.tsx, CanvasEditor.tsx, CanvasPanel.tsx, CanvasHome.tsx, index.ts}`; `database.ts` gained `canvas_notes` + `CanvasNote`. typecheck + lint + build clean. ⚠️ One-time: apply the migration (Supabase SQL Editor).
- [x] **Standalone canvases — project OR personal (2026-06-22).** Decoupled a canvas from projects. Migration `20260622200000_canvas_standalone.sql`: **(1)** `canvas_notes` gains `owner_id uuid → auth.users on delete cascade` (BACKFILLED from each canvas's project owner, then `default auth.uid()` + `not null`), `project_id` made **nullable** (a personal canvas has none), + a `(owner_id, updated_at desc)` index. **(2)** New **`canvas_members`** table mirroring `project_members` (`canvas_id`+`user_id` PK, role `editor`/`viewer`, `user_id` index) — the **owner is `canvas_notes.owner_id`, never a member row**; created now so the per-canvas **sharing UI (next prompt)** has its schema. **(3)** Five SECURITY DEFINER helpers (`security definer`/`search_path=''`/granted to authenticated): `user_is_pro`, `canvas_is_pro` (project → `project_is_pro`, personal → `user_is_pro`), `is_canvas_owner`, `can_access_canvas` (owner ∨ project member ∨ canvas_members), `can_edit_canvas` (owner ∨ project editor ∨ canvas_members editor). **(4)** `touch_canvas_notes()` extended to RAISE if `project_id`/`owner_id` change (immutable). **(5)** Canvas RLS rewritten — SELECT `can_access_canvas`; INSERT `(project + can_edit_project + project_is_pro)` OR `(personal + owner + user_is_pro)`; UPDATE `can_edit_canvas` + `canvas_is_pro`; DELETE project-editor OR owner. **(6)** `canvas_members` RLS — SELECT `can_access_canvas`, write `is_canvas_owner`. **Frontend:** `database.ts` (canvas_notes `project_id: string|null` + `owner_id`, `canvas_members`, `CanvasRole`/`CanvasMember`, 5 helper signatures); `api.ts` (`owner_id` in summary, `fetchAllCanvases`, `insertIndependentCanvas`); `useCanvas.ts` (aggregated `['canvas-all', userId]` + `useAllCanvases` labelling Personal vs project, `useCreateIndependentCanvas`, and `useSaveCanvas`/`useDeleteCanvas` now take `projectId: string|null` and reconcile the project **and** aggregated caches); `CanvasEditor` `projectId` widened to `string|null`; **`CanvasHome` rebuilt** from a project picker into the real full-width workspace (EntityPicker over every canvas + "+ New" personal canvas + inline lazy editor, `<ProGate>` on the viewer's own plan). `CanvasPanel` (per-project tab) unchanged — its writes just flow through `can_edit_canvas` now. Konva stays code-split (main bundle Konva-free; `CanvasEditor` chunk 324 kB/100 kB gz, shared by both entry points). typecheck + lint + build clean. ⚠️ One-time: apply the migration.
- [x] **Notes & Canvas full-width layout refactor (2026-06-22).** Layout/markup only — data, RLS, autosave, and Pro-gating untouched. Removed the two-column `lg:grid-cols-[18rem_minmax(0,1fr)]` + `<aside>` side list from both `NotesPanel` and `CanvasPanel`; each tab now has one header row — a compact glass **`EntityPicker`** (new `components/forms/EntityPicker.tsx`) + the count + a "+ New" GradientButton — over the selected editor at **full width on every breakpoint**. The picker matches `GlassSelect` (trigger = selected title + chevron; popover lists every note/canvas as title + subtitle; closes on select/outside/Escape; keyboard-navigable; reduced-motion aware). New shared **`SegmentedToggle`** (`components/forms/SegmentedToggle.tsx`) backs both editor toggles. `NoteEditor`: Edit/Preview toggle shown on all sizes (removed `lg:hidden`), single full-width active pane (removed `lg:grid-cols-2` + `hidden lg:block`), pane grows to fill. `CanvasEditor`: stage fills the tab (`relative min-h-[75vh] flex-1`, removed the fixed `h-[58vh] sm:h-[66vh]` + padded GlassPanel) and gained an Edit/View toggle (View = read-only pan/zoom, edit controls hidden, page-type a read-only indicator, zoom kept) via a derived `editing` flag. Removed the mobile back-arrow + `onBack` from both editors (→ `onDeleted`); kept most-recent-default, delete→fall-back-to-next, the loading/error/empty states, autosave (debounce + flush-on-unmount), save indicator, inline title, delete-confirm, and all RLS/Pro gating. Konva still code-split (main bundle unchanged; canvas chunk 329 kB/101 kB gz). New files: `components/forms/{EntityPicker.tsx, SegmentedToggle.tsx}`. typecheck + lint + build clean.
- [x] **Fix: canvas creation blocked by RLS — the "opens for half a second and closes" bug (2026-06-25).** Creating a canvas failed as a silent flash: the optimistic editor opened, the `INSERT into canvas_notes` was rejected by RLS (**Postgres `42501`** — "new row violates row-level security policy"), the optimistic cache rolled back, and the editor unmounted. **Diagnosed live on the deployed app** (it was NOT the full-width layout change the symptom suggested): verified `project_is_pro(project)=true` (RPC `200 true`) and `can_edit_project=true` (the owner has a `project_members` row), yet the insert still 403'd — so the **`canvas_notes` INSERT policy actually live in the database does not match the migrations.** The standalone-canvas migration's `owner_id` column + SELECT policy applied (reads work), but its write-policy step didn't fully land, leaving **no valid INSERT policy → RLS default-denies every canvas insert** regardless of Pro status. **Fix (2 parts): (1) DB** — new migration `20260625120000_fix_canvas_insert_rls.sql` re-asserts the correct `canvas_notes` INSERT/UPDATE/DELETE policies from `20260622200000` (idempotent; also re-asserts `owner_id default auth.uid()` + nullable `project_id`). ⚠️ **Must be applied in the Supabase SQL Editor** (see Current next step). **(2) Frontend** — create failures are no longer silent: new `src/features/canvas/errors.ts` (`canvasCreateErrorMessage` / `isRlsDenial`) + an inline `role="alert"` error banner wired into `CanvasPanel.tsx` and `CanvasHome.tsx`, so a rejected insert shows the RLS denial instead of a phantom flash. New files: `supabase/migrations/20260625120000_fix_canvas_insert_rls.sql`, `src/features/canvas/errors.ts`. (Sandbox file-mirror desynced again this session, so in-session `tsc` ran against truncated copies of the two edited files — changes verified by inspection; `npm run typecheck` is clean locally.) **⬇️ UPDATE — that wasn't the whole story; see next entry for the actual fix.**
- [x] **Fix (REAL root cause): canvas creation 42501 was the SELECT read-back, not the INSERT policy (2026-06-25).** After re-asserting the 4 policies, creating a canvas STILL returned `42501`. Decisive test: setting the INSERT policy to `with check (true)` **still** 42501'd (a `true` check can't be violated) — so the INSERT policy was never the blocker; loosening the **SELECT** policy fixed it. **Mechanism:** the app creates a canvas with `.insert(...).select()` → PostgREST runs `INSERT ... RETURNING *`; the RETURNING read-back is gated by the **SELECT** policy, which called `can_access_canvas(id)` — a **STABLE SECURITY DEFINER** function that **re-queries `public.canvas_notes`**. During `INSERT ... RETURNING`, that re-query doesn't see the just-inserted (uncommitted) row under the statement snapshot → `can_access_canvas` returns false → the read-back fails → `42501 "new row violates row-level security policy"`. A plain SQL `INSERT` (no RETURNING) never triggers the SELECT policy, which is why every direct DB test passed and only the app failed — the perfect red herring. **Fix:** migration **`20260625140000_fix_canvas_select_readback.sql`** rewrites the `canvas_notes` SELECT policy to check the row's **own columns inline** (`owner_id = auth.uid()` OR `is_project_member(project_id)` OR a `canvas_members` lookup) instead of re-querying via `can_access_canvas` — identical security, but it never re-reads the table being inserted, so the new-row read-back succeeds. **Verified live: canvas insert now returns 201 and the editor opens + persists, with the proper (non-loosened) INSERT + SELECT policies applied.** Along the way also dropped a rogue `canvas_stamp_owner` BEFORE-INSERT trigger that an earlier Supabase "Debug with Assistant" attempt had added (`new.owner_id := auth.uid()` — redundant with the column default, not the cause but cleaned up). **TODO for the codebase:** the `20260622200000` canvas migration still defines the SELECT policy as `can_access_canvas(id)` — `20260625140000` supersedes it; keep `20260625140000` in the migration history so fresh deploys get the inline policy. Frontend error-surfacing (`features/canvas/errors.ts` + banners) still pending a git commit.
- [x] **Pro P3.2 — pressure-sensitive freehand drawing (2026-06-26).** Real stylus/finger/mouse drawing on the canvas via **perfect-freehand** (already a dep). New tool modes (`CanvasTool = 'select' | 'draw' | 'erase'`): **draw** captures pointer events on the Konva Stage (`onPointerDown/Move/Up/Cancel`), collecting `[x, y, pressure]` **world** samples — **real pen pressure** when `pointerType==='pen'` (`simulatePressure:false`), simulated-from-velocity otherwise; high-rate styluses use `getCoalescedEvents()` for sub-frame smoothness. The in-progress stroke is drawn **imperatively on a dedicated, non-listening preview `<Layer>`** (no React render per move; `batchDraw`) and **committed to the scene on pointerup**. **Strokes are first-class `StrokeElement`s** (the foundation stub's `points`/`color`/`size` gained `thinning`/`smoothing`/`opacity`/`blend`/`simulatePressure`) stored in `scene` — selectable, movable, **resizable** (a resize bakes the scale into the sample points + size), lockable, z-ordered, undo/redo-able like any element; rendered as a Konva `<Path>` from `getStroke()` (recomputed from the stored samples so the stroke stays crisp at any zoom). **Pen toolbar** (`PenToolbar`, shown only in draw mode): three presets — **pen** (tapered), **marker** (near-uniform), **highlighter** (translucent + `globalCompositeOperation:'multiply'`) — plus a colour palette and a size slider (preset switch resets size to its default). **Eraser** = stroke-level: hit-tests via `stage.getIntersection` + `findAncestor('.canvas-element')`, hides touched strokes **live**, commits the whole batch as **one undo step** on release (only strokes, never text/image/media). **Palm rejection:** once a pen is used, touch is ignored for ~800 ms so a resting palm can't draw or pan. **No P3.1 regressions** — pan (drag-empty in select), pinch-zoom, wheel-zoom, selection + Transformer all preserved; draw/erase disable element drag/select + hide the Transformer; a second finger cancels an in-progress stroke and pinch-pans instead. Keyboard: `V`/`P`(or `B`)/`E` switch tools; Ctrl/⌘-Z/Y + Delete unchanged. TS strict (no `any`), reduced-motion safe. **Konva + perfect-freehand stay code-split** in the lazy `CanvasEditor` chunk (now 339 kB / 105 kB gz; main bundle unchanged at ~1094 kB). New files: `features/canvas/{freehand.ts, drawing.ts, PenToolbar.tsx}`; touched `elements.ts`, `constants.ts`, `elementRenderers.tsx`, `CanvasStage.tsx`, `CanvasToolbar.tsx`, `CanvasEditor.tsx`. **No migration** — the `scene` jsonb shape is forward-compatible and no stroke data existed before. typecheck + lint + build all clean.

---

## 🧠 Decision log

> One line per decision. Full reasoning lives in `plan.md`.

- **2026-06-29 (Pro P3.6 — canvas element management: pin/lock, z-order, multi-select, copy/paste, context menu, layers panel).** Full element-management polish. **Multi-select:** `selectedIds: string[]`; Shift+click toggles; Konva `Transformer.nodes([...])` attaches to all non-locked selected nodes. **Marquee:** Shift+drag on empty stage shows HTML rubber-band div; on release intersecting elements are selected; `stageRef.stopDrag()` prevents simultaneous pan. **Group drag:** `groupDragOrigins` ref + Stage onDragMove applies delta imperatively to all co-selected nodes; `handleGroupDragEnd` batches via `setTimeout(0)` into one `commit()`. **Batched changeElement:** `pendingPatches` Map + `setTimeout(0)` collapses transform-end events into one history step; `sceneElementsRef` prevents stale-closure captures. **Z-order:** `bringToFront/bringForward/sendBackward/sendToBack/reindexZ` added to `elements.ts`; toolbar shows 4 z-order buttons (Ctrl+]/[). **Pin/lock:** locked elements skip `draggable` + Transformer, faded in Layers panel; toggle in toolbar + context menu. **Visibility:** `visible: boolean` (Zod `.default(true)` for backward-compat); `ElementNode` returns null when hidden; eye toggle in Layers. **Duplicate:** `duplicateElements()` + Ctrl+D + context menu. **Copy/paste:** `clipboard: CanvasElement[]` state; paste creates fresh UUIDs offset +20 directly from clipboard (not `duplicateElements`, which looks up by id — copied elements may be deleted). **Context menu:** `ContextMenu.tsx` glass float at container-relative (x,y); triggered by Konva onContextMenu (right-click) or 600ms long-press timer; closes on outside pointerdown or Escape. **Layers panel:** `LayersPanel.tsx` absolute right overlay sorted z-desc; Eye/Lock per row; click selects, Shift+click toggles. **Keyboard:** Ctrl+C/V/D/]/[, Shift+]/[ (to-front/back), Delete, Arrow nudge (Shift=10px), Ctrl+Z/Y, all wired via `actionsRef` (always-current, avoids stale closure). **Touch parity:** long-press fires context menu, cancelled on move. typecheck clean; no regressions vs P3.1–P3.5.
- **2026-06-29 (Pro P3.4 — canvas images: signed URL cache, dynamic keepRatio, personal canvas exclusion).** Four key choices: **(1) Module-level signed URL cache** (`urlCache` + `inFlight` Maps in `elementRenderers.tsx`) — a module singleton prevents duplicate Supabase Storage requests when multiple `ImageVisual` mounts ask for the same path simultaneously; the dedup map (`inFlight`) is cleaned up on resolve/reject to prevent memory growth; no TTL — URLs are valid 1 hour and a full page reload is the refresh path. **(2) Manual `new window.Image()` load instead of `use-image`** — `use-image` is not installed; a `useEffect` in `ImageVisual` that sets/clears `onload`/`onerror` and a `cancelled` flag is the idiomatic substitute; it fires once per resolved URL and is cleaned up on unmount. **(3) Dynamic `keepRatio` on the Transformer** — `keepRatio` is computed each render as `selectedElement?.type === 'image'` (true for images, false for everything else) so images resize aspect-locked by default while text/stroke resize freely — without needing Shift to be wired up in Konva (which doesn't support that pattern natively). **(4) Personal canvases excluded** — Storage RLS parses `projectId` as `split_part(name,'/',1)::uuid` from the upload path, so a personal canvas (no `projectId`) would fail the cast; rather than hack around RLS, the image button is hidden when `projectId === null` and the paste/drop guards check the same condition. **(5) Paste guarded by `editingTextId`** — the outer container div's `onPaste` handler returns early if `editingTextId !== null` so Tiptap text boxes still receive paste events normally (Tiptap handles its own clipboard internally).

- **2026-06-26 (Pro P3.2 — freehand: store input samples, not the outline; one preview layer).** Persist each stroke's **raw `[x,y,pressure]` input samples** (local space) + the perfect-freehand params, NOT the computed outline polygon — so the filled outline is recomputed with `getStroke()` at render time and stays crisp at any zoom, and the model stays CRDT-friendly for P3.7. **Pressure fidelity is persisted:** `simulatePressure` is stored per stroke (false for a real stylus, true for finger/mouse) so a reload renders identically; `opacity`+`blend` carry the highlighter look (translucent + `multiply`) since the task's core list (points/color/size/thinning/smoothing) can't express it alone. **Perf = a dedicated, `listening:false` preview Layer** the stroke draws onto **imperatively** (set `.data()` + `batchDraw()` on a Path ref) during the gesture — zero React renders per pointer-move — then a single `commit()` to history on pointerup; coalesced pointer events recover full-rate stylus samples. **Tool modes over a mode flag:** `CanvasTool` became `select|draw|erase`; in draw/erase the Stage is non-draggable (1-pointer draws/erases) but **two-finger pinch + wheel still pan/zoom**, and a second finger cancels the stroke — so P3.1's pan/zoom/select is untouched, just gated by tool. **Palm rejection** = ignore touch for ~800 ms after any pen event (a `penActive` ref + timer), the standard pen-tablet behaviour. **Stroke-level erase** (whole stroke, batched into one undo) over pixel-erase: simpler, matches a notes canvas, and reuses Konva's hit graph (`getIntersection`); erased strokes hide live via an `erasingIds` set the editor filters, committed on release. **Resize bakes geometry:** a stroke has no inner shape to stretch, so transform-end multiplies the sample points + size by the node scale (the `ElementPatch` type was widened to carry `points`/`size`). perfect-freehand stays in the **lazy** canvas chunk (imported only by `freehand.ts`/the editor), so the main bundle never gains it.
- **2026-06-22 (Canvas decoupled from projects — a canvas is project OR personal).** A canvas no longer *requires* a project: `canvas_notes.project_id` is **nullable** and a new **`owner_id`** (immutable, defaulted to `auth.uid()`) makes a personal canvas owned by one user, while a project canvas keeps `project_id` and is shared by project membership. **Ownership = `owner_id`, never a member row** — exactly the `projects.owner_id` / `project_members` split — so a canvas is reachable three ways (owner, project membership, or a **`canvas_members`** row) resolved by SECURITY DEFINER helpers (`can_access_canvas`/`can_edit_canvas`/`is_canvas_owner`) so the `canvas_members` policies never self-recurse (the project_members gotcha). **`canvas_members` is created NOW but its sharing UI is the next prompt** — the schema lands early so the access model is complete and the migration is one-shot. **Pro-gating stays double but becomes plan-aware:** create/edit needs Pro on the *governing* plan — a project canvas on the **board owner** (`project_is_pro`), a personal canvas on **its owner** (`user_is_pro`) — and `canvas_is_pro` picks the right one for the UPDATE check; read + delete stay membership/owner-gated so a lapsed plan can still view + clean up (the canvas-media Storage philosophy). **Backfill before immutability:** existing rows get `owner_id` from their project owner *before* the trigger starts rejecting owner_id changes, so the migration is re-runnable. **`/canvas` became a real workspace, not a picker:** it aggregates **every** canvas the user can reach (RLS-scoped) — labelled Personal vs project — reusing the P3.1b EntityPicker + lazy editor; "+ New" makes a **personal** canvas (so the page is gated on the *viewer's own* plan, the one that governs personal creation). `useSaveCanvas`/`useDeleteCanvas` reconcile **both** the per-project list and the aggregated cache so the project tab and `/canvas` stay live; the per-project `CanvasPanel` was left untouched (a `string` projectId still satisfies the widened `string|null`). Konva stays code-split — `CanvasHome` lazy-imports `CanvasEditor` exactly like `ProjectPage` does `CanvasPanel`, so the main bundle stays Konva-free.
- **2026-06-22 (UI — Notes & Canvas tabs go single-pane + full width).** Reworked both per-project editor tabs so the editor uses the **whole width** and editing-vs-viewing is one toggle (markup/layout only — no data/RLS/autosave/Pro-gating touched). **Dropped the `lg:grid-cols-[18rem_…]` side list** (and the mobile list↔editor swap) in favour of a single **glass dropdown picker** to switch items — chosen over a persistent rail because the editor is the work surface and a dropdown reclaims the full width on every breakpoint (phone→desktop) identically. The picker is a **new shared `EntityPicker`** rather than reusing `GlassSelect` directly: the trigger must show only the title (not the two-line option) and it needs real **arrow-key list navigation**, so it's a purpose-built listbox on the same `.glass-menu` surface (generic `{id,title,subtitle}` items; closes on select/outside/Escape; reduced-motion via `useReducedMotion`). Its activeIndex reset happens in the open handler (not an effect) to satisfy the `set-state-in-effect` lint rule; only the `focus()` DOM call lives in an effect. **Notes** show the Edit/Preview toggle on **all** sizes (no more desktop side-by-side) and render only the active pane full-width; **Canvas** gains a parallel Edit/**View** toggle implemented as a single derived `editing = canEdit && mode==='edit'` flag fed to `CanvasStage`/`CanvasToolbar`/the keyboard effect — so View reuses the **existing `canEdit=false` read-only path** (pan/zoom only, edit controls hidden, page-type select disabled = a read-only page indicator, zoom kept) with zero new stage logic. The Konva stage now **fills** its tab (`min-h-[75vh] flex-1`, stage `absolute inset-0`) instead of a fixed `58/66vh` box, and lost its redundant padded GlassPanel wrapper (the stage draws its own bordered surface). Both editors **dropped `onBack`/the back-arrow** (no list to return to) and now take `onDeleted` to clear the parent selection; selection still defaults to most-recent and falls back to the next item on delete. A shared **`SegmentedToggle`** replaces the inline `ModeButton`. Konva stays code-split (canvas chunk 329 kB/101 kB gz; main bundle unchanged) since the two new shared form components are tiny and live in the main bundle. typecheck + lint + build all clean.
- **2026-06-22 (Pro P3.1 — Notes Canvas foundation built).** Implemented `prompts.md` P3.1 — canvas + pages + element scaffold + persistence only (no freehand/text/media bodies yet). **Two persisted doc representations side by side** on `canvas_notes`: **`scene jsonb`** (`{ elements: [...] }`, the denormalised array read directly today, the source of truth) and **`doc_state bytea`** (reserved, NULL until the Yjs CRDT snapshot lands in P3.7 — created now so the column exists). **The real Pro gate is RLS**, mirroring the canvas-media Storage philosophy: SELECT = `is_project_member` and DELETE = `can_edit_project` (so a lapsed-plan board can still view + clean up), but **INSERT/UPDATE add `project_is_pro`** (board owner's plan) so create/edit is Pro-only even via the raw API; the UI `<ProGate>` is UX-only. **Element model = a discriminated union** `CanvasElement = Stroke | TextBox | Image | Media` with a shared transform base (id/x/y/width/height/rotation/z/locked); fields for the later-phase bodies are declared now (stable persisted shape) but only a **TextBox stub** is creatable in P3.1. The untrusted `scene` jsonb is **Zod-parsed on read**, dropping garbage elements rather than crashing. **`page_type` is a column, not in `scene`** (and not in the undo stack); `PageType` lives in `lib/canvasPages.ts` (React-free) so `database.ts` + the feature share it like `AccentName`/`LabelColor`. **Background pattern drawn in WORLD space** inside the stage-transformed layer (not screen space) so it naturally pans + zooms with content; line width = 1/scale stays a crisp 1px, with density/Count guards when zoomed far out. **Pan/zoom/select**: the Stage is `draggable` (background layer `listening:false` so empty hits reach it → pan); wheel + 2-finger pinch zoom toward the pointer/centre; element Groups are individually draggable; a **Transformer** does resize/rotate, baking Konva scale back into width/height on transform-end. **Undo/redo = a swappable local snapshot command stack** (`useSceneHistory`, reducer past/present/future) — the interface (`scene`/`commit`/`undo`/`redo`/`reset`) is intentionally storage-agnostic so P3.7 swaps in `Y.UndoManager` without touching the editor. **Autosave mirrors the notes editor** (debounced 700ms + flush-on-unmount, `buildPatch` diffing) but the doc is held locally (history) and saved non-optimistically, reconciling the saved row into `['canvas', noteId]` + re-sorting the `['canvas-list', projectId]` summaries. **Konva is fully code-split**: the barrel exports only the Konva-free `CanvasHome`; `ProjectPage` `React.lazy`'s `CanvasPanel` via a direct dynamic import, so Konva/the editor land in a **separate 330 kB (101 kB gz) chunk** loaded only on canvas open — the main bundle is unchanged (Konva never ships to non-canvas users, per the bundle constraint). Palette colors for the canvas are resolved from CSS vars (`--fg`/`--accent-from`) on the stage container and recomputed on theme flip (`useCanvasPalette`). typecheck + lint + build all clean. ⚠️ One-time: apply `20260622180000_canvas.sql` in the Supabase SQL Editor.
- **2026-06-22 (Plan/ops — defer go-live to the very end; Pro P0–P2 + Dodo all applied & verified in TEST).** Decision: **finish BUILDING first (P3 canvas), then do all launch/go-live prep at the end** — don't flip live payments on mid-build. **State now:** every Pro migration is **applied** in Supabase — `20260622000000_pro_foundation` (P0), `20260622120000_dodo_billing`, `20260622140000_custom_reminders` (P1), `20260622160000_collaboration_pro` (P2), plus the earlier `20260621210000_billing` / `20260621230000_collab_limits_feedback_ceo`; reminders **pg_cron is at `*/10`**; and a **Dodo TEST checkout was verified end-to-end** (webhook flipped the account to Pro, `dodo_subscription_id` written). **Deferred to the FINAL "go-live" step (after P3):** Dodo **KYC / business verification**; add a **Refund/Cancellation policy** page + a **Contact** page; finalize **Terms/Privacy** (remove the template banner); switch Dodo **TEST → LIVE** (live products + keys + webhook, `DODO_PAYMENTS_ENVIRONMENT=live`) and redeploy `send-due-reminders` for prod email; optional **custom domain**; final **security pass + Lighthouse**. Mirrored in prompts.md → "Final step — Go-live" and pro-plan.html. **Next build: P3.1 (Notes Canvas).**
- **2026-06-22 (UI — custom on-brand date+time picker; no native OS controls left).** The due-date field used native `<input type="date">`/`<input type="time">`, whose **OS-drawn calendar/clock popups ignore the Aurora design** — the same problem already solved for `<select>` via `GlassSelect`. Built **`src/components/forms/DatePicker.tsx`**: a glass month-grid picker (pure **date-fns** — `startOfWeek(startOfMonth)`…`endOfWeek(endOfMonth)`, mirroring `features/calendar/dates.ts`) with prev/next-month nav, Today/Clear, selected = accent-gradient cell, today = accent-bold, and an optional inline **time selector built from three `GlassSelect`s** (12-h hour / minute / AM-PM ↔ `HH:mm`). **Same value contract** as the native inputs (`value` `YYYY-MM-DD`, `time` `HH:mm`, `onChange`/`onTimeChange`), so `DueDateField` (its only consumer) just swaps the inputs — nothing else changed. **Key choices:** (1) the panel **expands in-flow** (a glass disclosure), NOT an absolute popover — an absolute popover gets **clipped by the card modal's `overflow-y-auto`** scroll box, while in-flow expansion never clips and the modal just scrolls; styled as a floating glass card so it still reads as a popover. (2) **Escape uses a capture-phase document listener with `stopImmediatePropagation`** so it closes the picker WITHOUT also tripping the surrounding `Modal`'s bubble-phase Escape-to-close. (3) Full **keyboard grid nav** (arrows = ±1/±7 days, PageUp/Down = month, Enter/Space = select, Esc = close) via roving `tabIndex` + a `keyboardNav` ref so mouse month-nav never steals focus; reduced-motion calms the entrance. **Audit:** confirmed **zero native `<input type="date|time|datetime-local|color|month|week">` and zero native `<select>` remain in `src/`** (only doc-comment mentions) — the label colour picker was already custom swatches. Pure UI — no data/reminders-logic change. typecheck + lint + build clean.
- **2026-06-22 (Pro P1 — custom timed reminders built).** Implemented `prompts.md` Phase P1 without touching the free day-based path. **Two columns model the deadline:** `cards.due_date` (bare `YYYY-MM-DD`) stays what the board/calendar group by; new **`cards.due_at timestamptz`** is the *source of truth for the moment a card is due* and what offsets measure against (backfilled from `due_date` at **09:00 UTC** — no per-user tz, and existing cards predate timed reminders, so the exact instant is immaterial; documented in the migration). **Offsets are rows, not a column:** `card_reminders(card_id, offset_minutes>=0, channel∈{email,push}, created_by)` — a card has many. **The real Pro gate is RLS:** SELECT = `can_access_card`, but **INSERT additionally requires `card_project_is_pro(card_id)`** (a new SECURITY DEFINER helper that resolves card→project→`project_is_pro`, the **board owner's plan** governing), so a free user is rejected even via the raw API; UPDATE/DELETE need only `can_edit_card` (cleanup survives a lapsed plan, mirroring canvas-media). **Idempotency = a dispatch ledger** `card_reminder_dispatches(card_reminder_id, due_at, pk)` — each reminder fires **once per `due_at` instance and auto-re-arms** when `due_at` changes (a new instant has no row); RLS-enabled with **no policies** so only the service-role function (which bypasses RLS as a SECURITY DEFINER owner) reads/writes it. **Precise path RPCs** (service-role only, like Phase 9): `due_time_reminder_candidates(window)` returns each pending `channel='email'` reminder where `now() ∈ [due_at − offset, +window)` AND board-is-pro AND not-yet-dispatched; `mark_time_reminders_sent(ids)` records dispatches at the card's current `due_at`. **Cron rescheduled daily → every 10 min**, and the **same `send-due-reminders` function now runs both paths**: the day-based digest **self-dedupes per `due_date`** (`cards.reminder_sent_for`) so 10-min cadence never re-spams — it just sends each card sooner. **Browser path:** `useTimedReminders` (mounted inside `useDueReminders`) polls the user's `channel='push'` reminders (two typed queries, joined client-side — supabase-js can't type an embedded-column filter), then a 60-s scan fires one `Notification` per `(reminder, due_at)` within `[fire, due+5m]`, deduped in localStorage with a `timed:` prefix; gated by the **same** master toggle as the day-based notifications (permission + Profile switch + online). **UI:** `DueDateField` gained an optional **time picker** (Pro only → `due_at`); a `RemindersSection` (gated by `<ProGate>`) gives quick chips (15m/30m/1h/2h/1 day) + a custom value+unit + a channel selector — free users see the upgrade card. **`due_at` threads through board + calendar exactly like `due_date`** (caches/mutations/optimistic patches; calendar drag preserves the card's time onto the new day). **Dropdown bug fixed everywhere:** extracted a reusable **`GlassSelect`** (the `.glass-menu` custom-dropdown pattern) and converted the **last two native `<select>`s** (ReminderSettings lead-time + CalendarToolbar project scope) — no native `<select>` elements remain in `src/`. typecheck + lint + build all clean. ⚠️ One-time Supabase steps (see Next step).
- **2026-06-22 (Payments — replaced Stripe with Dodo Payments, a Merchant of Record).** Stripe was never activated, so retiring it is data-safe. **Why Dodo:** as an **MoR** it is the seller of record — it localizes the checkout currency and **remits sales tax/VAT** for us (Stripe leaves tax to the merchant), which matters for a globally-sold solo product. **Architecture mirrors the old Stripe flow exactly** (same security posture): the verified webhook is the ONLY thing that flips `profiles.plan`; the browser is denied by the `protect_plan_columns` trigger (now guarding `dodo_*`). Functions call the **Dodo REST API directly** from Deno (no SDK; Bearer API key; base host chosen by `DODO_PAYMENTS_ENVIRONMENT`, default test) — `dodo-create-checkout` creates a **Checkout Session** (`POST /checkouts`) for the monthly/annual **product id from env** (never hardcoded — ids differ test vs live: test `pdt_0NhalBvSKlS70L1sUMkur` / `pdt_0Nhalv2OMQi73YMxXuxm8`), passing `customer` (reuse stored `dodo_customer_id` else email/name) + `metadata.user_id` so the webhook can map back; `dodo-portal` returns the customer-portal `link`. **Webhook security:** `dodo-webhook` (deployed `--no-verify-jwt`) verifies the **Standard Webhooks** signature manually with SubtleCrypto (`base64(HMAC-SHA256(base64decode(secret_after_whsec_), "{id}.{ts}.{body}"))`, ≤5-min timestamp tolerance, constant-time compare) **before** any DB write. **Event → plan map:** `subscription.active`/`renewed` → `plan='pro'`, `plan_status='active'`, store `dodo_customer_id`+`dodo_subscription_id`; `on_hold` → `plan_status='past_due'` (keep Pro during dunning); `cancelled`/`expired`/`failed` → `plan='free'` (**never** grant Pro on `failed` — terminal). Profiles resolved by `metadata.user_id` (persists on the subscription), falling back to stored `dodo_subscription_id`/`dodo_customer_id`. Prices stay **USD $5.99 / $68.29** (display only — Dodo localizes at checkout). No Dodo secret ever ships to the browser — all 5 secrets live only in Supabase Edge secrets.
- **2026-06-22 (Pro P0 — gating + storage foundation built).** Implemented `prompts.md` Phase P0. **The real gate is `project_is_pro(p_project_id uuid)`** — a `SECURITY DEFINER` / `set search_path = ''` helper (migration `20260622000000_pro_foundation.sql`) that resolves `projects.owner_id → profiles.plan = 'pro'`, so **the board owner's plan governs** a shared board (same rule as the member limit), mirroring the `is_project_member` / `can_edit_project` pattern; granted to `authenticated`. First use of **Supabase Storage**: a **private** `canvas-media` bucket (path `‹projectId›/‹noteId›/‹file›`, served via signed URLs) with RLS on `storage.objects` — **any member may SELECT** (so signed URLs work, and media stays viewable if a plan lapses), but **INSERT/UPDATE/DELETE require `is_project_member` AND `project_is_pro`** for the projectId parsed via `split_part(name,'/',1)::uuid` (every policy guarded by `bucket_id='canvas-media'` first so the cast only runs on our objects). **Size caps:** the bucket `file_size_limit` is a hard **100 MiB** ceiling (the largest per-type cap); the finer per-type caps (image 10 / audio 25 / video 100 MB) + MIME allow-list live in `src/lib/proFeatures.ts` and are enforced **client-side** in `src/lib/storage.ts` before upload — a single bucket limit can't express per-type byte caps and `metadata.size` isn't reliable at policy time for resumable uploads (documented in supabase/README). **UI gate (UX only):** `useIsPro()` (reads `useProfile().plan === 'pro'`) + a `<ProGate>` component (renders children for Pro, else a glass upgrade card opening the existing `UpgradeModal`); both exported from the billing barrel. `proFeatures.ts` is the single source of *which* features are Pro (with a `shipped` flag so unbuilt features aren't marketed) + the media caps; `plans.ts` now pulls the shipped Pro labels from it so they can't drift. `project_is_pro` added to the hand-maintained `database.ts` Functions. **No existing policy touched/weakened.** typecheck + build clean; new files lint-clean (5 pre-existing lint errors remain in untouched files: `billing/api.ts`, `members/api.ts`, `ProjectsPage.tsx`, `useOnlineStatus.ts`). **Unblocks P1–P3.** ⚠️ One-time: apply the migration in the Supabase SQL Editor.
- **2026-06-21 (Plan — Pro feature roadmap: collaborative Notes Canvas + custom timed reminders + Pro collaboration).** Wrote **[prompts.md](./prompts.md)** — a phased build playbook (P0 gating/storage → P1 timed reminders → P2 collaboration → P3 canvas, sub-phased P3.1–P3.7), in the `prompt.md` house style. **Locked decisions:** (1) canvas engine = **custom** (`react-konva` + `perfect-freehand` + `Tiptap` + custom media elements) — **tldraw rejected** ($6,000/yr commercial license), **Excalidraw rejected** (no native rich text / audio-video / page types); (2) **live multiplayer = Yjs** — one `Y.Doc`/note synced over a **thin custom Supabase-Realtime broadcast provider** + awareness cursors, Tiptap text bound to `Y.XmlFragment`s, persisted as `canvas_notes.doc_state bytea`; (3) audio/video = **both** in-app record/upload (Supabase Storage, Pro-gated, size-capped) **and** allow-listed embeds (YouTube/Vimeo/Loom/SoundCloud); (4) collaboration = **all four** (threaded comments, @mentions + notifications, review/approval, activity log, reactions). **Pro-gating is double:** `useIsPro()` in the UI + a `project_is_pro()` SECURITY DEFINER helper enforced in RLS + Storage policies (the **board owner's plan governs**, like the member limit). Introduces the **first use of Supabase Storage** (`canvas-media` private bucket). The canvas module must be **lazy-loaded** (bundle already over the warning). **Nothing built yet — plan + prompts only.**
- **2026-06-21 (Pricing — Pro set to $5.99/mo + an annual plan at 5% off).** `src/lib/plans.ts` stays the single source: `priceMonthly` 8→**5.99**, new `priceAnnual` = **$68.29** (12×5.99 − 5%, ≈ $5.69/mo effective), `PRO_ANNUAL_DISCOUNT_PCT = 5`, + helpers (`planPrice`/`annualPerMonth`/`annualSavings`) and a `BillingInterval` type. **A second (yearly) Stripe Price is now required:** checkout threads `interval` (`'month'|'year'`) from a new **`IntervalToggle`** (Monthly/Annual segmented switch with a "Save 5%" pill) shown on the **BillingPage** upsell + the **UpgradeModal**, through `useBilling`→`api.createCheckoutUrl(interval)`→the `create-checkout-session` Edge Function, which selects **`STRIPE_PRICE_PRO`** (monthly) vs **`STRIPE_PRICE_PRO_ANNUAL`** (yearly) — and **falls back to monthly if the annual secret is unset**, so nothing breaks before the price exists. Marketing `PlanCard` now shows the annual line; `.env.example` + supabase/README document the new secret. **Economics (after Stripe 2.9% + $0.30):** ≈ **$5.52/mo → $66.20/yr** from a monthly subscriber vs **$66.01/yr** from an annual one — essentially identical (the 5% discount ≈ the 11 saved $0.30 flat fees), but annual gives cash upfront + lower churn. ⚠️ **Same sandbox-mount corruption — not built/committed here;** run `npm run typecheck && npm run build`, create the **yearly** Stripe price + `supabase secrets set STRIPE_PRICE_PRO_ANNUAL`, then commit.
- **2026-06-21 (UX — feedback discoverability + mobile search polish; + pricing research).** Users had nowhere obvious to give feedback: the form existed only as a popup in the user menu, and the `/feedback` tab was `adminOnly` (the inbox). Fix: extracted a shared **`FeedbackForm`** (used by both the popup and the new page), added **`FeedbackPage`** (submission form for everyone; admin also sees the inbox below), routed `/feedback` → `FeedbackPage`, and made the nav item visible to all (label **"Feedback/Features"**, `MessageSquareHeart` icon; kept the user-menu popup as a quick shortcut). **Mobile top search bar** read as cramped (font near top/bottom edges) — bumped the Topbar search affordance + the board toolbar search `h-10`→`h-11` (the design-system input height, matching `Field`) with `leading-none`. **Pricing research (delivered in chat):** the stack is effectively free at current scale (Cloudflare Pages = unlimited static bandwidth; Supabase free = 50k MAU / 500 MB db / 1 GB storage / 5 GB storage-egress). The planned Pro features (note images, **voice recording**, custom reminders) add **no build/compute cost** — voice **storage + egress** is the only real cost driver, and only once you exceed the free 1 GB / 5 GB-egress, at which point Supabase Pro is $25/mo (overage $0.021/GB storage, $0.09/GB egress). Custom reminders ride the existing email Edge Function (Resend free = 3k/mo). Recommended Pro price **$5.99–$6.99** (the $0.30 Stripe flat fee is ~6% of $4.99, so $4.99 is viable but thin; $8 in code today is also defensible) + an **annual** option. `src/lib/plans.ts` still says **$8** — left unchanged pending the user's decision. ⚠️ **Build/lint/commit could NOT run:** the sandbox FS mount served NUL-corrupted/stale files (same flaky-mount issue as 2026-06-18/06-20), so `tsc`/`eslint`/`git` are unreliable here; real-disk files are correct (verified via the editor). **Run `npm run typecheck && npm run build` + commit on a stable machine.**
- **2026-06-21 (Phase 10 — launch: marketing site + Stripe billing + plan limits).** **Plan = a column on `profiles`, not a table** (`plan` ∈ {free,pro} + `stripe_customer_id`/`stripe_subscription_id`/`plan_status`) — one row per user, read everywhere via the new `current_plan()` helper and `src/lib/plans.ts` (the single source of plan limits/features). **Limits are enforced in the DB, never the UI:** a `projects` BEFORE INSERT trigger blocks a free user's 4th owned project (`PROJECT_LIMIT_REACHED`), which the client catches to show the `UpgradeModal`; the UI also proactively gates the New-project button + shows `n/3 used`. **Users can't self-upgrade:** the own-row `profiles` UPDATE policy would otherwise let a user set `plan='pro'`, so a second BEFORE UPDATE trigger rejects any change to the billing columns unless `auth.jwt()->>'role' = 'service_role'` — i.e. only the **verified Stripe webhook** (service role) flips the plan; the post-checkout client redirect is never trusted. **Three Edge Functions:** `create-checkout-session` + `create-portal-session` (auth'd, return a Stripe URL the SPA redirects to) and `stripe-webhook` (deployed `--no-verify-jwt`; verifies the signature with `constructEventAsync` + `createSubtleCryptoProvider()` on Deno *before any write*; handles checkout.session.completed / customer.subscription.updated / deleted). **Routing split for a public front door:** `/` serves the marketing `LandingPage` to signed-out visitors (signed-in → redirect to `/boards`), so the dashboard moved from `/` to **`/boards`** (updated nav, the `?new=1` trigger, breadcrumbs, leave-project + post-login redirects). **Marketing site reuses the REAL components** (CardSurface/Avatar/LabelPill/glass) for faux board/calendar/notes previews so it looks identical to the product; built as `features/marketing` by a subagent against the locked `plans.ts` + design contracts. **ToS/Privacy are templates** (lawyer-review banner) — see the README go-live checklist before charging.
- **2026-06-21 (post-launch)** — **Invitations now require explicit accept/decline (not auto-join), + UI/offline polish.** An invite stays **pending** until the invitee acts: dropped the `auth.users` signup auto-redeem trigger; added SECURITY DEFINER `accept_invitation(id)` (creates the membership + consumes the invite, returns the project id) and `my_pending_invitations()` (lists the invitee's invites **with project name** — the members-only projects RLS would otherwise hide it before joining); added an **invitee-delete** policy (decline) and a **member self-delete** policy (leave; the owner stays trigger-protected). Frontend: removed the on-load `redeem_my_invitations`; a `PendingInvitations` banner on Boards (`useMyInvitations`/`Accept`/`Decline`) + a **Leave project** action in `MembersPanel`. **Dropdowns:** native `<select>` option lists can't be rounded/recoloured (the OS draws them), so role + assignee pickers are now **custom dropdowns** on the opaque **`.glass-menu`** surface (layered tint over `rgb(var(--bg))` so nothing bleeds through, both themes). **Offline:** `navigator.onLine` reports online on a "connected, no-internet" network (common on Windows), so `useOnlineStatus` also **probes `${SUPABASE_URL}/auth/v1/health`** (no-cors) — that's why the banner never fired before. **Auth redirect:** the app already passes `redirectTo: origin()`; the real fix was Supabase **URL config** (Site URL = the pages.dev domain + both `localhost:5173` and pages.dev in the redirect allow-list) so dev and prod each return to their own origin. **Reminders Edge Function bug:** the `rpc()` helper `JSON.parse`d every response, but `mark_reminders_sent` returns void (empty body) → "Unexpected end of JSON input" *after* a successful send; now it only parses a non-empty body.
- **2026-06-21** — **Phase 9 PWA & reminders.** **SW = `registerType: 'prompt'` + `useRegisterSW`** (not `autoUpdate`): a Workbox `generateSW` precaches the app shell (`globPatterns`, `navigateFallback: index.html` for SPA/offline routes, `maximumFileSizeToCacheInBytes` lifted to 4 MB since the single bundle is ~950 kB pre-split), and a custom **PWAReloadPrompt** offers a one-tap reload so updates never blow away a mid-edit — `injectRegister` auto-detects the virtual import so there's **no double registration** (confirmed: no inline `registerSW` in built HTML). **Icons are generated, not bundled deps** — `scripts/generate-icons.mjs` hand-encodes PNGs (built-in `zlib` + a CRC32 table, per-pixel aurora gradient + SDF "A" glyph) → 192/512/maskable/apple-touch + a vector `favicon.svg`; keeps the toolchain dependency-free and regenerable. **theme-color** is one dynamic `<meta id="theme-color">` updated by `applyTheme()` (+ the pre-paint inline script) rather than two media-query metas, because the app has a **manual** theme toggle that can disagree with the OS preference. **Offline read-only view = persisted Query cache, not SW runtime-caching of Supabase** — `PersistQueryClientProvider` + a localStorage `createSyncStoragePersister` (`gcTime` raised to 24 h ≥ `maxAge`; `staleTime` left at 0 so online behavior/Realtime are unchanged; only **successful** queries dehydrated). Chosen over caching raw authenticated API responses on disk (privacy on shared devices, and DELETE/RLS staleness); the cache is **wiped on sign-out** (`clearPersistedCache` in AuthProvider's `SIGNED_OUT`). **Queued offline writes deliberately skipped** ("if feasible" in the brief) — a correct write queue across optimistic caches + RLS conflicts is a large, error-prone surface; offline is read-only with a clear banner, writes fail-and-rollback (Phase 10+ candidate). **Reminders = "Both" (user-chosen):** the **reliable** path is an **email Edge Function on pg_cron**, the **zero-setup** path is **browser notifications** — they're complementary, not either/or, and neither adds frontend bundle weight (the function is server-side; notifications are ~the Notification API). **All reminder logic that needs `auth.users` (emails) or cross-user reads lives in two SECURITY DEFINER RPCs** (`due_reminder_candidates`, `mark_reminders_sent`) with **EXECUTE revoked from anon/authenticated and granted only to `service_role`**, so the browser can never enumerate emails; the Edge Function is additionally **secret-gated** (`x-cron-secret`) and deployed `--no-verify-jwt` (the cron, not a user, calls it). **Dedupe is `cards.reminder_sent_for date`** (the due_date last emailed) — `is distinct from due_date` re-arms a card automatically when its due date changes, no trigger needed. **Reminder secrets (`RESEND_API_KEY`/`REMINDER_FROM_EMAIL`/`CRON_SECRET`) are NOT `VITE_` vars** — they live only in Supabase Edge secrets (documented in `.env.example` + supabase/README). **Assignee picker added now** (was reserved since Phase 4) because reminders target a card's assignee: `AssigneeField` reuses the Phase 8 `useMembers` cache, and `assignee_id` threads through the **existing** `updateCardDetail` path so board **and** calendar persist it with no new mutation (same rationale as priority in the 2026-06-20 batch). `supabase/functions` + `scripts` added to ESLint ignores (Deno/Node, outside the app's typed graph).
- **2026-06-20** — **Phase 8 Collaboration (invitations, roles, realtime, presence).** **Invitations are keyed by email**, not user id, because the invitee may not have an account yet — an `invitations(project_id, email, role∈{editor,viewer}, invited_by)` row is **consumed into a `project_members` row** the moment that email becomes a user. Two redemption paths share one SECURITY DEFINER core (`redeem_invitations_for(user, email)`, a `delete…returning`→`insert…on conflict do nothing` atomic move): a **signup trigger** on `auth.users` auto-joins brand-new users, and a **`redeem_my_invitations()` RPC** (called from `ProjectsPage` on load) covers already-registered invitees. Redemption matches on the **authenticated user's own email on file** — the standard Supabase pattern; no email-ownership re-check (note for the security pass before charging). **Roles are enforced in RLS, not just the UI** (the brief's hard constraint): reads stay `is_project_member`, but every content **write** now gates on **`can_edit_project(project_id)`** (owner/editor) — and card-children on **`can_edit_card(card_id)`** — so viewers are read-only at the database; the UI threads a `canEdit` prop purely for polish (hidden compose/drag/edit affordances + a read-only card view + read-only notes). **The owner is sacrosanct:** ownership = `projects.owner_id`, and a `protect_project_owner_membership` BEFORE trigger forbids demoting/removing the owner's `project_members` row or granting `owner` to anyone else — with a **null-guard** (`projects` row already gone ⇒ it's a project-cascade delete ⇒ allow) so deleting a whole project still works. `profiles` got a second, additive SELECT policy via **`shares_a_project_with()`** so co-members can read each other's name/avatar (own-row-only would show ids). **Realtime = debounced cache invalidation, not surgical patching:** `useProjectRealtime` subscribes to `postgres_changes` for the active project and, on any event, **debounce-invalidates** the matching project-scoped TanStack key (board / card-extras / notes / members). Refetch is the authoritative, RLS-filtered snapshot → no duplicates, no flicker (TanStack swaps only on real change), conflict-tolerant, and **no cross-project leak** (every invalidation/refetch is keyed to this `projectId`; Realtime's own RLS limits which events even arrive). Chosen over per-event patching because DELETE payloads carry only the row identity and the card-child tables (`checklist_items`/`card_labels`) have no `project_id` to patch by — for those two we subscribe unfiltered and just refresh **this** project's extras (`REPLICA IDENTITY FULL` lets the project_id filters match on deletes for the rest). **Presence** uses Realtime Presence on a per-project channel keyed by user id (multi-tab collapses to one person) to show live viewers. **Members data is two queries, not a PostgREST embed** (`project_members.user_id`→`auth.users`, so there's no FK to embed `profiles` across), joined client-side. **Email is normalised to lowercase at the schema** so it matches the `lower(email)` unique index + redemption lookup with a plain equality filter.
- **2026-06-20** — **Phase 7 Notes/docs.** A `notes` row is **project content**, so it reuses the multi-tenant pattern **verbatim** — `project_id` + RLS on every op gated by **`is_project_member(project_id)`**, no new helper. **`updated_at` is server-owned:** a `before update` trigger (`touch_notes_updated_at`, `search_path=''`) stamps it on every write, so the list's "newest-edited-first" order and the "saved" time never trust the client (autosave fires many UPDATEs). **Autosave design:** the editor holds local title/content state, derives save status **during render** (no `setState` in effects — the repo's react-hooks lint forbids it), and **debounces 700 ms**; the actual write only runs inside the timeout / mutation callbacks. A **flush-on-unmount** effect (a ref-held closure, written in an effect — never during render) saves a pending edit when you switch notes inside the debounce window, so nothing is lost. Optimistic writes go through one `['notes', projectId]` cache (board/todos pattern); `mutate` is destructured for a **stable** reference so background re-renders can't reset the debounce timer. **Markdown is hand-rolled, on purpose:** `markdown.tsx` parses a common subset to **React elements** — XSS-safe by construction (React escapes text; no `dangerouslySetInnerHTML`; link hrefs allow-listed to http/https/mailto/relative) — chosen over a library because the brief said avoid heavy deps, the bundle is already over the warning, and a renderer (not a rich-text editor) is the smaller, safer surface. **Tabs are URL-driven** (`?tab=notes` via `useSearchParams`, replace) so the global `/notes` picker (`NotesHome`) can deep-link straight to a project's notes. **react-hooks lint gotcha (new this session):** the repo's ESLint flags (a) writing/reading a ref's `.current` during render and (b) synchronous `setState` inside an effect body — both must move to event handlers / async callbacks / effects; derive display state during render instead.
- **2026-06-20** — **Daily to-do planner = a private, non-project feature.** Lists/items belong to **one user**, not a project, so RLS is the simple **own-rows** pattern (like `profiles`), not membership. `todo_lists.user_id` **defaults to `auth.uid()`** so inserts omit it; `todo_items` gate through a new SECURITY DEFINER **`owns_todo_list(list_id)`** helper (resolves the list's owner once — same "keep policies flat" rationale as `can_access_card`). The model is **per-day, multi-list**: a `(list_date, name)` list holds items, so a day can have several named lists (Personal/Work). State is one cache **per day** (`['todos', dateKey]` → `{lists, items}`), optimistic like the board. No drag-reorder (append by fractional `position`); kept out of the project nav as its own sidebar tab + `/todos` route.
- **2026-06-20** — **Task priority is an open-ended integer, not an enum.** Per the request ("P1…P10 and beyond if more tasks"), `cards.priority` is a nullable `integer >= 1` (1 = most urgent, NULL = unset) — no fixed set, so big boards can use P11+. UI: `PriorityField` offers P1–P10 quick chips + a "Higher…" number input; `lib/priority.ts` is the single source of `formatPriority`/tier→color used by both the picker and the card-face pill. It **rides the existing `updateCardDetail`** path (added one column to the patch), so the board's `useUpdateCard` and the calendar's `useUpdateCalendarCard` both persist it with no new mutation. **Card-modal add bug (same day):** the checklist + label composers were `<form>`s nested in the modal's `<form>` — invalid HTML that made "+" submit the outer card form; the fix (here and the pattern going forward) is **no nested `<form>`s** — list composers use a div + click/Enter handlers.
- **2026-06-18** — **Phase 6 Calendar view:** No schema change — the calendar is a **read/rewrite over `cards.due_date`** (populated since Phase 5). **One query feeds the whole view:** `fetchDatedCards()` selects every card with a non-null `due_date` (RLS scopes it to the user's projects), and the **scope** (all-projects vs one project) is a client-side filter, so switching scope costs nothing. **Cross-cache mutations** are the key design: a card lives simultaneously in `['calendar-cards']`, its project's `['board', id]`, and `['card-extras', id]`, so reschedule/edit/delete patch **all relevant caches optimistically** (and invalidate them on settle) — drag a card to a new day here and it's already moved when you open the board. **Drag-to-reschedule** uses dnd-kit's low-level `useDraggable`/`useDroppable` (not Sortable — there's no intra-day order to persist): each chip is draggable, each `DayCell` is a droppable keyed by its `toDateKey` (`YYYY-MM-DD`), and drop sets `due_date` to that key. Only the in-grid chip registers a draggable for a given id; the lifted clone, agenda rows, and overflow-modal rows reuse the **presentational `CardChip`** (no hook) so there's never a duplicate-draggable-id. **Chips encode two signals at once:** project identity (accent gradient bar, via `accentVars`) and urgency (a danger/warning dot from the Phase 5 `due.ts`). **Clicking a chip opens the exact Phase 5 `CardDetailModal`** (imported from `features/board`), wired to the calendar's own mutations — full reuse, no fork. **Responsiveness** is a JS media-query split (`useMediaQuery('(min-width:640px)')`) rather than CSS `hidden`, specifically so the month grid's dnd droppables and the agenda don't both mount; mobile gets a tap-to-open agenda where rescheduling happens via the card's existing date picker. **Date math is all date-fns**, local-time throughout (`toDateKey`/`isToday`/`parseISO` all align with `due.ts`'s `startOfToday`), so a card sits on the same day it was set to regardless of timezone — no heavy calendar lib. **Filename gotcha:** the helpers file was first named `calendarGrid.ts`, which collides with the `CalendarGrid.tsx` component on case-insensitive Windows/macOS filesystems (tsc errors) — renamed to `dates.ts`. **Reduced-motion** gates the drag-overlay spring; the grid otherwise relies on the global reduced-motion CSS.
- **2026-06-18** — **Card delete wired + working-tree corruption recovery (bugfix):** Cards couldn't be deleted. The data layer (`removeCard` in `api.ts` / `useDeleteCard` in `useBoard.ts`) has existed since Phase 4 but was **never surfaced in the UI** — `CardDetailModal` only had Cancel/Save and `Board` never imported `useDeleteCard`. Added a destructive **Delete** action to the modal footer (left of Cancel/Save) with an **inline confirm** step (no nested modal), following the column-delete convention (`accent="ember"` + `AlertTriangle`, `text-danger`). Wired `useDeleteCard` into `Board` via `handleDeleteCard` (delete → close modal) plus a new **`useRemoveCardExtras(projectId)`** helper in `useCardExtras.ts` that drops the deleted card's checklist items + label links from the `['card-extras']` cache, mirroring the DB's `on delete cascade`. Touched only `CardDetailModal.tsx`, `Board.tsx`, `useCardExtras.ts`. **Incident:** while verifying, `src/features/projects/ProjectPage.tsx` (760 NUL bytes on one line) and `src/types/database.ts` (truncated at line 113) were found **corrupted in the working tree but clean at HEAD** — restored both via `git show HEAD:<path> > <path>`. Cause looked like a flaky file mount (also broke `git status` with a bad `…0000` object and fed `tsc` shifting truncation errors); the fix was therefore verified by **direct file inspection** rather than a clean typecheck. **TODO on a stable machine: `git status`, `npm run typecheck && npm run build`, then live-test deleting a card.**
- **2026-06-18** — **Phase 5 Card details (usable MVP):** Three new tables. `labels` is project-scoped (gated on **`is_project_member(project_id)`** — the verbatim pattern); `checklist_items` and `card_labels` hang off a **card**, which carries `project_id` but the rows don't, so rather than have their policies sub-query `public.cards` (re-entering cards' RLS) they gate through a **new SECURITY DEFINER helper `can_access_card(card_id)`** that resolves the card's project once and reuses `is_project_member` — same hardening (`search_path=''`, schema-qualified) and same "keep policies flat" rationale as the Phase 3 helpers. **This is the pattern any future card-child table (comments, attachments) reuses.** Label **color is a name** (`violet`…`slate`) constrained by a CHECK, mapped to a hex in **`src/lib/labelColors.ts`** — mirrors how `projects.accent` is stored; keeps the DB decoupled from exact swatches. **State = one `['card-extras', projectId]` cache** ({labels, checklist, cardLabels}) parallel to `['board', id]`: the board reads it for card-face label pills + checklist progress, the modal reads + mutates the same snapshot (instant optimistic, unit rollback). New-label-then-attach uses **`mutateAsync` then attach with the real id** to avoid an FK race on the temp id. **Due date** is part of the card row, so it saves through the board cache (extended `useUpdateCard`/`updateCardDetail` to write `due_date`), not card-extras; urgency (`due.ts`) is overdue→danger / ≤2 days→warning / else neutral, "due this week" = next 7 days. **Filtering hides non-matching cards with `display:none`** (kept mounted in the SortableContext) instead of removing them, so drag `neighbourPosition` still reads the full ordered lists and never mis-positions relative to a hidden card. Checklist reorder is a **nested dnd-kit DndContext** (the modal is a sibling of, not inside, the board's DndContext, so no context clash), reusing `positionBetween`. `database.ts` hand-extended with the three tables + `can_access_card` + `ChecklistItem`/`Label`/`CardLabel` aliases.
- **2026-06-18** — **Phase 4 Kanban:** `columns`/`cards` are the first per-project content tables and reuse the Phase 3 pattern **verbatim** — RLS on every op gated by **`is_project_member(project_id)`**, so no new recursion-avoidance work. **Membership = full board access** for now; viewer-read-only and role limits arrive with collaboration (Phase 8). New projects get **To Do / In Progress / Done** via a `seed_project_columns()` SECURITY DEFINER `after insert` trigger (independent of `on_project_created`, so firing order is irrelevant); a one-time backfill seeds pre-existing projects. **Ordering = fractional `position`** (`double precision`): an item moved between two neighbours takes the **midpoint** of their positions, so a reorder writes **one row** and never collides — chosen over full reindex for cheap, trivially-optimistic moves. Float64 midpoints survive ~50 inserts into the *same* gap; a periodic rebalance is deferred to Phase 10. All position math lives in **`ordering.ts` + `Board.tsx`**; mutations take explicit positions and stay dumb. **State = one `['board', projectId]` TanStack cache** ({columns, cards}) so the DnD layer and every mutation read/patch one consistent snapshot (instant optimistic updates, unit rollback). **dnd-kit**: Mouse(distance 6) + Touch(delay 200/​tol 8) + Keyboard sensors (taps open a card, swipes scroll, long-press drags); `closestCorners`; cross-column card moves are previewed live in `onDragOver` (canonical multi-container pattern) while within-column + column reflow comes from applied sortable transforms; the lifted clone is a **`DragOverlay`** (scale/rotate via Framer spring). **Confetti** (`Confetti.tsx`, keyed-remount burst, no effect setState) fires only when a card *enters* a Done-type column (`isDoneColumn` name match), gated by reduced-motion. `database.ts` hand-extended with `columns`/`cards` + `Column`/`Card` aliases.
- **2026-06-18** — **Phase 3 RLS bugfix (found in live verification):** creating a project returned **403 / 0 rows**. Root cause: on `INSERT ... RETURNING`, Postgres applies the `projects` **SELECT** policy to the new row, but the creator's `owner` row in `project_members` (written by the `after insert` trigger) is **not yet visible** at that instant, so a membership-only SELECT policy failed the read-back and rolled the whole insert back. Fix: broadened the `projects` select policy to `using (owner_id = auth.uid() or is_project_member(id))` — the owner clause is true the moment the row exists, so RETURNING succeeds. Migration `20260618120000_projects.sql` patched + applied live. (Also: this project's Supabase uses the **legacy anon JWT** key, not the new `sb_publishable_` key, which was rejected as "Invalid API key".) **Later per-project tables that rely on the same create-then-return pattern should gate SELECT on the owning row's creator too, not membership alone.**
- **2026-06-18** — Phase 3 multi-tenant RLS: **membership is the unit of access.** A row is visible only to members of its project, checked via the **`is_project_member(project_id)`** SECURITY DEFINER helper; ownership-gated writes use a sibling **`is_project_owner(project_id)`** helper. Both are `security definer`/`set search_path = ''` so their bodies read the tables as the table owner (bypassing RLS), which is what **breaks the `project_members` self-recursion gotcha** (plan.md §6) — policies call the functions instead of sub-querying `project_members`. The creator is made an `owner` member by an `after insert` SECURITY DEFINER trigger (`handle_new_project()`), not client logic, so the trigger can insert the membership before the "owners-only" member policy would otherwise allow it. **This is the canonical pattern every later table (columns, cards, notes, …) reuses.** Accent stored as `text` with a CHECK to the six gradient names; typed as `AccentName` in `database.ts` (hand-maintained). Projects dashboard replaced the placeholder `Home`; project list/detail use TanStack Query with **optimistic** create/update/delete (temp `crypto.randomUUID()` swapped for the server row on success, rolled back on error).
- **2026-06-18** — Phase 2 auth: **session state lives in `AuthProvider`** (Supabase `getSession()` + `onAuthStateChange`; supabase-js handles persistence + token refresh). **Profile data is separate** — a TanStack Query (`useProfile`) shared by the nav `UserMenu` and the profile screen, so an edit updates both. `profiles` is protected by **own-row RLS** (`select`/`update`, `authenticated` role only); inserts happen solely via the **`handle_new_user()` SECURITY DEFINER trigger** (search_path pinned to `''`, all refs schema-qualified). Client route guards (`ProtectedRoute`/`PublicOnlyRoute`) are **UX only — RLS is the real protection** (plan.md §6). Added a **reset-password completion screen** (`/reset-password`) so the Forgot-Password flow actually works end-to-end. `src/types/database.ts` is **hand-maintained** to mirror the migration until regenerated via the Supabase CLI.
- **2026-06-18** — Phase 1 theming runs on **CSS variables** (channel triplets wired into Tailwind tokens) under `.dark`/`.light` on `<html>`; an inline bootstrap script in `index.html` sets the class pre-paint (no flash). Theme switch cross-fades via the **View Transitions API** (falls back to a CSS transition; disabled under reduced-motion). Added libs: **@fontsource-variable** (self-hosted fonts, PWA-friendly), **lucide-react** (icons), **clsx + tailwind-merge** (the `cn()` helper). Accent system lives in `src/lib/accents.ts` (the six gradients + `accentVars()`); components read `--accent-*` so any subtree can be re-accented.
- **2026-06-18** — Phase 0 used **Tailwind CSS v3** (mature `tailwind.config.ts` + `postcss.config.js` token workflow) rather than v4's CSS-first config, since Phase 1's design system (`plan.md` §4) is built around config tokens. Scaffold built **in place** (no subfolder); path alias `@/* → src/*`; PWA kept minimal (full manifest/offline in Phase 9).
- **2026-06-18** — Phase 1 now ends with a **live-deploy checkpoint** (verify the `*.pages.dev` URL on desktop + phone) to de-risk deployment early.
- **2026-06-18** — Design **elevated** per request: light + dark both first-class (persisted, cross-fade); animated **flowing gradients**, **3D tactile buttons** (raised, hover-lift, press), and **pointer-reactive card tilt**. Spec in `plan.md` §4.4; `prompts.html` demos it.
- **2026-06-18** — Web app delivered as **PWA** (one codebase, sync, sellable). 
- **2026-06-18** — Auth = **email/password + Google** (Supabase Auth).
- **2026-06-18** — Host = **Cloudflare Pages**; backend = **Supabase**. Avoid Vercel Hobby (bans commercial use).
- **2026-06-18** — **No Docker** — nothing self-hosted to containerize (`plan.md` §2).
- **2026-06-18** — Design = **Aurora**: animated gradients + glassmorphism + vivid per-project accents + spring motion.

*(Add new decisions on top as they happen.)*

---

## 🗂️ Current file structure

```
Project Management app/
├── .env.example          ← documents the two VITE_ Supabase vars (real .env is gitignored)
├── .gitignore            ← ignores node_modules, dist, dev-dist, .env*, OS/editor cruft
├── .prettierrc.json      ← Prettier config
├── .prettierignore
├── CLAUDE.md             ← working rules (read every session)
├── SETUP.md              ← one-time setup: accounts, keys, git remote, Cloudflare
├── README.md             ← run-locally + Cloudflare Pages deploy guide
├── plan.md               ← spec (architecture, design, data, security)
├── memory.md             ← this file (state)
├── prompt.md             ← build prompts per phase (Phases 0–10, the core app)
├── prompts.md            ← Pro features build playbook (Notes Canvas, custom reminders, collaboration)
├── prompts.html          ← interactive prompt tracker — core app Phases 0–10 (open in browser)
├── pro-plan.html         ← interactive Pro-plan tracker — checkboxes + progress bar for the 10 Pro prompts
├── index.html            ← Vite entry HTML
├── package.json          ← deps + scripts (dev/build/preview/typecheck/lint/format)
├── eslint.config.js      ← ESLint flat config (TS + react-hooks, no-any)
├── postcss.config.js     ← Tailwind + autoprefixer
├── tailwind.config.ts    ← Tailwind theme (Aurora tokens land in Phase 1)
├── vite.config.ts        ← Vite + React + PWA plugin, @ alias
├── tsconfig.json         ← solution config (references app + node)
├── tsconfig.app.json     ← strict app TS config
├── tsconfig.node.json    ← TS config for vite/tailwind config files
├── supabase/
│   ├── README.md         ← how to apply migrations + the one-time Auth provider/redirect setup
│   └── migrations/
│       ├── 20260618093000_profiles.sql  ← profiles table + handle_new_user() trigger + own-row RLS
│       ├── 20260618120000_projects.sql  ← projects + project_members + is_project_member()/is_project_owner() + creator-as-owner trigger + multi-tenant RLS
│       ├── 20260618140000_kanban.sql    ← columns + cards + member-gated RLS + seed-default-columns trigger + backfill
│       ├── 20260618160000_card_details.sql ← checklist_items + labels + card_labels + can_access_card() helper + member-gated RLS
│       ├── 20260620100000_card_priority.sql ← cards.priority (open-ended integer ≥1) + index — 2026-06-20
│       ├── 20260620120000_todos.sql        ← todo_lists + todo_items + owns_todo_list() helper + own-row RLS — 2026-06-20
│       ├── 20260620140000_notes.sql        ← notes table + touch_notes_updated_at() trigger + member-gated RLS — 2026-06-20
│       ├── 20260620160000_collaboration.sql ← invitations + redemption (RPC + signup trigger) + can_edit_project/can_edit_card/shares_a_project_with helpers + owner-protect trigger + role-aware write RLS + profiles co-member SELECT + Realtime publication — Phase 8
│       ├── 20260621090000_reminders.sql    ← profiles reminder prefs + cards.reminder_sent_for + due_reminder_candidates()/mark_reminders_sent() (service-role-only RPCs) — Phase 9
│       ├── 20260621180000_invitation_accept.sql ← accept/decline invitations (no auto-join) + leave project — post-launch
│       ├── 20260621210000_billing.sql       ← profiles plan + billing cols + current_plan() + project-limit & billing-column-protect triggers — Phase 10
│       ├── 20260621230000_collab_limits_feedback_ceo.sql ← member-limit trigger + feedback + ceo_messages + is_admin() — Phase 10.1
│       ├── 20260622000000_pro_foundation.sql ← project_is_pro() helper + private canvas-media bucket + storage.objects RLS (member read; member+pro write) — Pro P0
│       ├── 20260622120000_dodo_billing.sql  ← stripe_*→dodo_* profile cols + extended protect_plan_columns guard — Dodo billing
│       ├── 20260622140000_custom_reminders.sql ← cards.due_at + card_reminders + card_reminder_dispatches + card_project_is_pro() + due_time_reminder_candidates()/mark_time_reminders_sent() (service-role-only) — Pro P1
│       ├── 20260622160000_collaboration_pro.sql ← comments + comment_mentions + reactions + activity_log + notifications + cards.review_* + RLS/definer triggers + Realtime — Pro P2
│       ├── 20260622180000_canvas.sql           ← canvas_notes (page_type/scene/doc_state) + touch trigger + Pro-gated RLS — Pro P3.1
│       └── 20260622200000_canvas_standalone.sql ← canvas owner_id + nullable project_id + canvas_members + user_is_pro/canvas_is_pro/is_canvas_owner/can_access_canvas/can_edit_canvas + immutability trigger + rewritten canvas RLS — standalone canvases
├── supabase/functions/
│   └── send-due-reminders/index.ts ← Deno Edge Function: runs BOTH the day-based digest AND the precise channel='email' timed path; secret-gated, Resend, marks sent/dispatched (cron every 10 min) — Phase 9 + P1
├── scripts/
│   └── generate-icons.mjs    ← zero-dep PNG icon generator (aurora gradient + "A" glyph) — Phase 9
├── public/                   ← static assets (Phase 9): pwa-192x192.png, pwa-512x512.png, maskable-512x512.png, apple-touch-icon.png, favicon.svg
└── src/
    ├── App.tsx                 ← routes (AppShell layout → Boards / To-Do / Calendar / Notes / StyleGuide)
    ├── main.tsx                ← React root + Theme/Query/Router providers + font imports
    ├── vite-env.d.ts           ← typed import.meta.env (VITE_SUPABASE_*)
    ├── components/
    │   ├── index.ts            ← barrel export
    │   ├── AuroraBackground.tsx← drifting blobs + pointer parallax + grain
    │   ├── Avatar.tsx          ← gradient-initials / image avatar
    │   ├── Badge.tsx           ← status pills (tones)
    │   ├── Modal.tsx           ← spring glass modal / mobile sheet (portal, Esc, scroll-lock)
    │   ├── glass/              ← GlassPanel, GlassCard (3D tilt)
    │   ├── buttons/            ← GradientButton (3D tactile, variants/sizes)
    │   ├── forms/              ← Field, TextArea, GlassSelect (reusable .glass-menu dropdown — replaces native <select>), DatePicker (glass calendar+time picker — replaces native date/time inputs)
    │   ├── feedback/           ← Spinner, Skeleton
    │   ├── motion/             ← Reveal wrapper
    │   ├── theme/              ← ThemeProvider, theme-context, ThemeToggle
    │   ├── shell/              ← AppShell (mounts useDueReminders + OfflineBanner + PWAReloadPrompt), Sidebar, SidebarNav, Topbar, BottomNav, Brand, navItems
    │   └── pwa/                ← OfflineBanner (offline indicator), PWAReloadPrompt (SW ready/update toast) — Phase 9
    ├── features/
    │   └── auth/               ← Phase 2 auth feature module
    │       ├── index.ts            ← barrel (provider, guards, pages, UserMenu)
    │       ├── auth-context.ts     ← AuthContext + value type
    │       ├── AuthProvider.tsx    ← session state (onAuthStateChange + getSession)
    │       ├── api.ts              ← supabase.auth wrappers + friendlyAuthError()
    │       ├── schemas.ts          ← Zod schemas + fieldErrorsOf()
    │       ├── identity.ts         ← resolveDisplayName/avatar from profile|user
    │       ├── useProfile.ts       ← profile query + update mutation (TanStack)
    │       ├── AuthLayout.tsx      ← shared glass auth card (+ OrDivider, AuthLink)
    │       ├── GoogleButton.tsx    ← "Continue with Google"
    │       ├── FormNotice.tsx      ← inline error/success/info banner
    │       ├── FullScreenLoader.tsx← guard fallback while session restores
    │       ├── ProtectedRoute.tsx  ← redirect unauthenticated → /login
    │       ├── PublicOnlyRoute.tsx ← redirect authenticated away from auth pages
    │       ├── SignUpPage.tsx · LoginPage.tsx · ForgotPasswordPage.tsx · ResetPasswordPage.tsx
    │       ├── ProfilePage.tsx     ← minimal display-name editor
    │       └── UserMenu.tsx        ← top-bar avatar → Profile / Sign out
    │   └── projects/           ← Phase 3 projects feature module
    │       ├── index.ts            ← barrel (ProjectsPage, ProjectPage)
    │       ├── api.ts              ← Supabase data layer (fetch/insert/patch/remove; RLS-governed)
    │       ├── useProjects.ts      ← TanStack hooks (useProjects/useProject + optimistic create/update/delete)
    │       ├── schemas.ts          ← Zod project form schema + fieldErrorsOf()
    │       ├── ProjectsPage.tsx    ← dashboard (index route): grid of cards, empty/loading/error states
    │       ├── ProjectCard.tsx     ← vivid Aurora glass card, stretched link, owner edit/delete actions
    │       ├── ProjectFormModal.tsx← create/edit modal (key-remount re-seeds; no reset effect)
    │       ├── AccentPicker.tsx    ← six-gradient radiogroup picker
    │       ├── DeleteProjectDialog.tsx ← owner-only delete confirmation
    │       └── ProjectPage.tsx     ← /projects/:id route (accent-themed header + Board|Notes tab via ?tab=notes)
    │   └── board/             ← Phase 4 Kanban + Phase 5 card-details feature module
    │       ├── index.ts            ← barrel (Board)
    │       ├── ordering.ts         ← fractional position helpers (positionBetween/neighbour) + isDoneColumn
    │       ├── due.ts              ← due-date urgency status/label helpers (date-fns) — Phase 5
    │       ├── schemas.ts          ← Zod column-name / card / checklist-text / label-name schemas
    │       ├── api.ts              ← board Supabase data layer (fetchBoard + column/card CRUD + moves; updateCardDetail writes due_date)
    │       ├── cardExtras.api.ts   ← Phase 5 data layer (labels / checklist_items / card_labels; RLS-governed)
    │       ├── useBoard.ts         ← TanStack hooks on one ['board', id] cache; optimistic add/rename/delete/move (incl. due_date)
    │       ├── useCardExtras.ts    ← TanStack hooks on one ['card-extras', id] cache; optimistic label/checklist/attach ops
    │       ├── Board.tsx           ← DndContext orchestrator (sensors, drag handlers, DragOverlay, confetti, toolbar, filtering, modals)
    │       ├── BoardToolbar.tsx    ← Phase 5 filter/search bar (title search + due chips + label chips)
    │       ├── BoardColumn.tsx     ← sortable column: grip, inline rename, delete, card list, quick-add composer
    │       ├── BoardCard.tsx       ← sortable card wrapper; passes face (labels + checklist) + hidden
    │       ├── CardSurface.tsx     ← presentational glass card: priority pill, label swatches, urgency due pill, checklist tally (also DragOverlay clone)
    │       ├── LabelPill.tsx       ← presentational colored label pill (card face / modal / filter)
    │       ├── CardDetailModal.tsx ← edit title/description/due-date + Priority + Labels + Checklist sections
    │       ├── DueDateField.tsx    ← native date picker, on-brand, with clear + urgency hint
    │       ├── PriorityField.tsx   ← priority picker: P1–P10 chips + "Higher…" custom — 2026-06-20
    │       ├── AssigneeField.tsx   ← assignee picker (project members); reminder target — Phase 9
    │       ├── CardLabelsSection.tsx ← attach/detach labels + inline create/delete (swatch picker)
    │       ├── Checklist.tsx       ← progress bar + reorderable list (nested dnd-kit) + add composer
    │       ├── ChecklistItemRow.tsx ← one to-do: grip, tick box, click-to-edit text, delete
    │       ├── DeleteColumnDialog.tsx ← confirm column (+ its cards) delete
    │       ├── AddColumn.tsx       ← trailing add-column composer
    │       └── Confetti.tsx        ← reduced-motion-aware celebration burst
    │   └── calendar/          ← Phase 6 Calendar view feature module
    │       ├── index.ts            ← barrel (CalendarPage)
    │       ├── api.ts              ← fetchDatedCards (all dated cards, RLS-scoped) + updateCardDueDate
    │       ├── useCalendar.ts      ← ['calendar-cards'] query + reschedule/update/delete (cross-cache optimistic: calendar + board + card-extras)
    │       ├── dates.ts            ← date-fns helpers (month/week day arrays, toDateKey, periodLabel, groupCardsByDate)
    │       ├── CalendarPage.tsx    ← DndContext orchestrator: view/scope/cursor state, grid↔agenda, card + day-overflow modals
    │       ├── CalendarToolbar.tsx ← title + period, month/week toggle, prev/today/next, project scope select
    │       ├── CalendarGrid.tsx    ← 7-col day grid (desktop/tablet; month + week variants)
    │       ├── DayCell.tsx         ← droppable day: accent today-pill, chips, "+N more" overflow
    │       ├── CardChip.tsx        ← presentational accent/urgency chip + DraggableCardChip (dnd-kit) wrapper
    │       ├── AgendaList.tsx      ← small-phone agenda (days-with-cards, tap to open)
    │       └── DayCardsModal.tsx   ← all cards for a day (the "+N more" popover)
    │   └── todos/             ← Daily to-do planner feature module — 2026-06-20
    │       ├── index.ts            ← barrel (TodosPage)
    │       ├── api.ts              ← fetchTodos(date) + list/item CRUD (own-row RLS)
    │       ├── useTodos.ts         ← ['todos', dateKey] cache + optimistic list/item ops
    │       ├── schemas.ts          ← Zod list-name / item-text schemas
    │       ├── TodosPage.tsx       ← date nav + grid of lists + add-list composer
    │       ├── TodoListCard.tsx    ← one named list: rename/delete + items + add-item composer
    │       └── TodoItemRow.tsx     ← one to-do: tick + text + delete
    │   └── notes/             ← Phase 7 per-project Notes/docs feature module
    │       ├── index.ts            ← barrel (NotesPanel, NotesHome)
    │       ├── api.ts              ← notes Supabase data layer (fetch/insert/patch/remove; RLS-governed)
    │       ├── useNotes.ts         ← ['notes', projectId] cache + optimistic add/update/delete
    │       ├── schemas.ts          ← Zod note title / content schemas (mirror DB constraints)
    │       ├── markdown.tsx        ← self-contained XSS-safe markdown→React renderer (no dep)
    │       ├── NotesPanel.tsx      ← two-pane list + editor (responsive list↔editor swap), create (canEdit-gated)
    │       ├── NoteEditor.tsx      ← title rename + markdown textarea + live preview + debounced autosave + delete (read-only when !canEdit)
    │       └── NotesHome.tsx       ← global /notes route: project picker → each project's Notes tab
    │   └── reminders/         ← Phase 9 due-date reminders + Pro P1 custom timed reminders
    │       ├── index.ts            ← barrel (useDueReminders, ReminderSettings, RemindersSection)
    │       ├── api.ts              ← fetchMyDueSoonCards (assignee + due-window filtered, RLS-scoped)
    │       ├── notifications.ts    ← browser Notification helpers (incl. generic showNotification) + reactive enabled-pref hook + localStorage dedupe
    │       ├── useDueReminders.ts  ← app-wide poll → day-based notification per card+due-date; also mounts useTimedReminders (AppShell)
    │       ├── useTimedReminders.ts← P1: poll the user's channel='push' reminders → fire once per (reminder, due_at)
    │       ├── offsets.ts          ← P1: quick-pick offsets, offset/channel labels, Zod offset+channel schemas
    │       ├── cardReminders.api.ts← P1: card_reminders CRUD + fetchUpcomingPushReminders (two typed queries, joined)
    │       ├── useCardReminders.ts ← P1: optimistic ['card-reminders', cardId] cache (add/remove)
    │       ├── RemindersSection.tsx← P1: ProGate'd offset editor (chips + custom + channel) for the card modal
    │       └── ReminderSettings.tsx← Profile section: email/browser toggles + lead-time GlassSelect
    │   └── members/           ← Phase 8 Collaboration feature module
    │       ├── index.ts            ← barrel (MembersBar, MembersPanel, useMembers/useMyRole, useProjectRealtime, useRedeemInvitations)
    │       ├── api.ts              ← members(+profiles join, two queries) / invitations CRUD / role change / remove / redeem RPC (RLS-governed)
    │       ├── useMembers.ts       ← ['members', projectId] cache ({members, invitations}) + optimistic invite/role/remove/cancel; useMyRole()
    │       ├── schemas.ts          ← Zod invite email(normalised lowercase) + role schema
    │       ├── useProjectRealtime.ts ← postgres_changes → debounced, project-scoped TanStack invalidation (board/extras/notes/members)
    │       ├── usePresence.ts      ← Realtime Presence: live board viewers (per-project channel keyed by user id)
    │       ├── useRedeemInvitations.ts ← redeem_my_invitations() RPC on app load (once/user) + refresh projects
    │       ├── RoleControl.tsx     ← RoleBadge (static) + RoleSelect (owner role editor↔viewer)
    │       ├── MembersBar.tsx      ← header avatar stack (presence ring + "N viewing") + Share/Members button → panel
    │       └── MembersPanel.tsx    ← modal body: roster (avatar/role/remove), pending invitations (cancel), invite form
    │   └── canvas/            ← Pro P3 Notes Canvas (lazy-loaded; Konva never ships to non-canvas users)
    │       ├── index.ts            ← barrel — exports ONLY the Konva-free CanvasHome
    │       ├── api.ts              ← canvas_notes data layer (list/all/fetch/insert/independent/patch/remove; RLS)
    │       ├── useCanvas.ts        ← ['canvas-list'|'canvas-all'|'canvas'] caches + optimistic create/save/delete
    │       ├── useCanvasPalette.ts ← resolve CSS-var colours to Konva strings (per theme/accent)
    │       ├── schemas.ts          ← Zod canvas-title schema
    │       ├── errors.ts           ← canvas-create error → friendly message + RLS-denial detector
    │       ├── elements.ts         ← CanvasElement union (Stroke/Text/Image/Media) + Zod parseScene + ElementPatch + topZ + Z-order helpers; P3.4: createImageElement factory; P3.6: visible field, bringToFront/sendToBack/bringForward/sendBackward/duplicateElements/reindexZ
    │       ├── freehand.ts         ← P3.2: perfect-freehand geometry — strokePathData() + buildStroke()
    │       ├── drawing.ts          ← P3.2: pen presets (pen/marker/highlighter) + colours + PenSettings + penStrokeStyle()
    │       ├── constants.ts        ← Camera + CanvasTool ('select'|'draw'|'erase') + zoom/scale consts; P3.6: NUDGE_STEP/NUDGE_LARGE_STEP/LONG_PRESS_MS
    │       ├── history.ts          ← useSceneHistory snapshot undo/redo stack (swappable for Y.UndoManager in P3.7)
    │       ├── PageBackground.tsx  ← world-space page pattern (blank/ruled/grid/dotted)
    │       ├── elementRenderers.tsx← ElementNode: stroke <Path> from getStroke() + resize-bakes-into-points; P3.4: ImageVisual (signed URL cache module-level urlCache/inFlight Maps + manual HTMLImageElement load; skeleton + error fallback)
    │       ├── CanvasStage.tsx     ← Konva stage: pan/zoom/select + pointer draw/erase + preview layer + palm rejection; P3.4: onDropFiles + keepRatio; P3.6: selectedIds[] multi-select, multi-node Transformer, marquee (Shift+drag), long-press context menu, group drag batching, onContextMenu prop
    │       ├── CanvasToolbar.tsx   ← floating glass toolbar: tools, undo/redo, add, page-type, zoom, lock/delete; P3.4: onAddImage; P3.6: z-order buttons (4), duplicate, layers toggle
    │       ├── ContextMenu.tsx     ← P3.6: glass float context menu (right-click/long-press); z-order/duplicate/copy/paste/lock/delete; closes on outside pointerdown or Escape
        │       ├── LayersPanel.tsx     ← P3.6: glass overlay element list sorted by z desc; per-row visibility+lock toggle; click selects, shift-click multi-selects
        │       ├── PenToolbar.tsx      ← P3.2: draw-mode pen toolbar (presets + colour palette + size slider)
    │       ├── CanvasEditor.tsx    ← stateful editor: history, autosave, tool/pen state, stroke commit, batched erase; P3.4: image upload; P3.6: selectedIds[] multi-select, clipboard copy/paste, contextMenu state, showLayers, batched changeElement (pendingPatches+setTimeout), handleGroupMove, z-order actions, keyboard shortcuts (Ctrl+C/V/D/]/[, arrows, Delete), toggleLock/Visibility
    │       ├── CanvasPanel.tsx     ← per-project Canvas tab (lazy-imported by ProjectPage)
    │       └── CanvasHome.tsx      ← /canvas workspace: aggregated picker + lazy editor (Pro-gated)
    ├── hooks/
    │   ├── useTheme.ts         ← theme context hook
    │   ├── useAuth.ts          ← auth context hook
    │   ├── useMediaQuery.ts    ← subscribe to a CSS media query (Calendar grid↔agenda switch) — Phase 6
    │   └── useOnlineStatus.ts  ← online/offline state (useSyncExternalStore) for the offline banner + reminder polling — Phase 9
    ├── lib/
    │   ├── supabase.ts         ← typed Supabase client (reads VITE_ env, throws if missing)
    │   ├── proFeatures.ts      ← single source: which features are Pro + canvas media caps + bucket name — Pro P0; P3.4: media.shipped = true
    │   ├── storage.ts          ← canvas-media upload/signed-URL helper; validates type+size vs caps (typed MediaUploadError) — Pro P0
    │   ├── queryClient.ts      ← shared QueryClient + localStorage persister (offline cache) + clearPersistedCache — Phase 9
    │   ├── accents.ts          ← six accent gradients + accentVars()
    │   ├── labelColors.ts      ← label palette (8 named colors → hex) + withAlpha() — Phase 5
    │   ├── priority.ts         ← open-ended task priority: format + tier→color (P1+) — 2026-06-20
    │   ├── dueAt.ts            ← cards.due_at date/time combine+split + formatClockTime helpers (local↔ISO) — Pro P1
    │   ├── cn.ts               ← clsx + tailwind-merge helper
    │   ├── motion.ts           ← spring presets + variants
    │   └── theme.ts            ← theme storage/apply logic
    ├── pages/                  ← StyleGuide, Placeholder
    ├── styles/
    │   └── index.css           ← Aurora tokens (both themes), glass/button/gradient utilities, blobs, reduced-motion
    └── types/
        ├── database.ts         ← Supabase DB types (`profiles`, `projects`, `project_members`, `columns`, `cards` [+`priority`], `checklist_items`, `labels`, `card_labels`, `todo_lists`, `todo_items`, `notes`, `invitations` + RLS fn signatures incl. `can_access_card`, `owns_todo_list`, `can_edit_project`, `can_edit_card`, `shares_a_project_with`, `redeem_my_invitations`; hand-maintained until CLI regen) + `Profile`/`Project`/`ProjectMember`/`ProjectRole`/`InvitationRole`/`Column`/`Card`/`ChecklistItem`/`Label`/`CardLabel`/`TodoList`/`TodoItem`/`Note`/`Invitation` aliases
        ├── fontsource.d.ts     ← module decls for @fontsource CSS imports
        └── view-transitions.d.ts ← optional startViewTransition typing
```

---

## ⚠️ Open items / known issues

- **🔴 Cloudflare build FAILED for P3.6 (commit `ceab267`).** The deployment shows "No deployment available" in the Cloudflare Pages dashboard. Most likely causes: (1) `lint.txt` was accidentally committed and may confuse the build, or (2) a Vite/tsc build error in the new P3.6 files that only surfaces during `vite build` (not caught by `tsc --noEmit` alone). **Fix:** open the Cloudflare Details log for `ceab267`, read the error, fix locally, then: `git rm lint.txt && git add -A && git commit -m "fix: remove lint.txt, resolve build error" && git push`. Live app remains at P3.5 until resolved.

- **Visual eyeball still pending.** The build/typecheck/lint pass and routes serve 200, but the design was not viewed in a browser this session (no GUI here). Run `npm run dev` and open `/style-guide` to confirm both themes, the 3D buttons, card tilt, and flowing gradients look right before building features on top (per `prompt.md` Phase 1 verification).
- **Cloudflare Pages — connected & live (2026-06-21):** https://project-management-app-dev.pages.dev (React (Vite) preset, `npm run build` → `dist`, both `VITE_` env vars set; auto-deploys on push to `main`). App loads + connects to Supabase. *Still nice to do: open the `*.pages.dev` URL on an actual phone to confirm install + responsive.*
- **Supabase keys:** local `.env` still holds placeholders — set real values (Settings → API) before auth can talk to the project.
- **Apply the Phase 2 migration & configure Auth (one-time, dashboard).** Run `supabase/migrations/20260618093000_profiles.sql` (SQL Editor or `supabase db push`), then enable **Email** + **Google** providers and add **redirect URLs** (localhost + `*.pages.dev`) — steps in [supabase/README.md](./supabase/README.md). Until done, the auth screens render but sign-in calls will fail.
- **Phase 2 live verification still pending** (needs real keys + a browser): sign-up auto-creates a `profiles` row, login persists across refresh, Google OAuth round-trip, protected-route redirect, and confirming you can't read another user's profile via SQL (RLS). See prompt.md → Phase 2 Verification.
- **Apply the Phase 3 migration (one-time, dashboard).** Run `supabase/migrations/20260618120000_projects.sql` (SQL Editor or `supabase db push`) after the profiles one. No new dashboard config needed beyond Phase 2.
- **Phase 3 live verification — partially done (2026-06-18).** Real keys now in `.env`, both migrations applied live, Email provider on. Confirmed: sign-in works, profile row auto-created, session persists on reload, and **creating a project now works** (after the 403/RETURNING-policy bugfix above) — shows with the Owner badge. **Still pending: the two-user RLS checks** — with two test users confirm: creating a project auto-adds the creator as an `owner` member; a non-member sees nothing (`select * from projects` returns only their own); a non-owner member can read but not update/delete; only the owner can add/remove members; and crucially that selecting/inserting on `project_members` raises **no** "infinite recursion detected in policy" error. Then eyeball the dashboard — card accents, create/edit/delete, optimistic updates, the accent picker, and the per-project route — in a browser (both themes).
- **Apply the Phase 4 migration (one-time, dashboard).** Run `supabase/migrations/20260618140000_kanban.sql` after the earlier two (SQL Editor or `supabase db push`). No new dashboard config. It also **backfills default columns** into any project created before Phase 4, so existing boards appear immediately.
- **Phase 4 live verification still pending** (needs a browser): open a project → board shows To Do/In Progress/Done; add/rename/delete a column; quick-add cards; open a card and edit title/description (persists + optimistic); drag a card within a column, across columns, and reorder columns (smooth on mouse **and** touch, persists on reload); dropping a card into **Done** fires confetti (and respects reduced-motion); confirm a non-member still can't read another project's columns/cards (RLS) and that no `project_members` recursion error appears.
- **Apply the Phase 5 migration (one-time, dashboard).** Run `supabase/migrations/20260618160000_card_details.sql` after the earlier three (SQL Editor or `supabase db push`). No new dashboard config. It adds `checklist_items` + `labels` + `card_labels`, the `can_access_card()` helper, and their RLS.
- **Phase 5 live verification still pending** (needs a browser): open a card → add checklist items, tick them, watch the progress bar + the card-face "3/5" update, reorder items, reload persists; set a due date → the card pill shows the right urgency color 