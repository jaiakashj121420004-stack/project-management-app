# dependencies
node_modules/
# build output
dist/
dev-dist/
*.tsbuildinfo
# env & secrets — NEVER commit
.env
.env.local
.env.*.local
# local payment-provider notes (Dodo/Stripe keys & product ids) — NEVER commit
Aurora*api*.txt
Aurora*ks*.txt
Aurora DODO PAYMENTS ks.txt
# supabase CLI local cache (project ref, image versions) — not source
supabase/.temp/
# misc
.DS_Store
*.log
.vscode/
.idea/
Aurora api.txt

# throwaway staging area for files the device bridge cannot delete outright
# (mv-not-rm quirk) -- never commit its contents
_to_delete/
